# crules · 评审（活跃文档）

> **单一权威**。历史版本全文见 [`archive/`](archive/)；每轮变更见顶部 Changelog。
> 本文档仅为评审，未改动任何规则或代码文件。

---

## 文档维护规则

- **已确认落地 / 已决议的项** → 在 [`待办.md`](待办.md) 标 ✅，并移入本文件「已关闭 / 已决议」表
- **已关闭积累到量或里程碑**（如某大建议整套落地、或 >8 条）→ 批量移 `archive/`，本文件只留 Changelog 一句话摘要
- 本文件维持 **「Changelog（行即详情——单行成段、允许 ①②③ 嵌套与版本链归因，v71 口径）+ 详情入口 + 已关闭」** 精简形态（待办自 v44 起独立于 [`待办.md`](待办.md)）；已关闭不占主视野，但 archive 全文 + Changelog 摘要双保险，不丢审计链
- 符号：🔴 重要 / 🟡 中等 / 🟢 小优化；✅ 已落地；🔒 已决议锁定（不再列为待收）
- **总览同步检查（防派生滞后，dogfooding）**：模板文件增删 / 能力新增的改动，收尾**必查**根 `README.md` + 子目录 `README.md` 的「文件清单 / 规模表 / 启用决策表」是否反映；未反映 = 改动未完成。查询源：[`CHANGELOG.md`](CHANGELOG.md)

### 元账本触发即更新表（v13续-A，v55 落地——MAINTENANCE 式同构，治「元账本同步靠记忆」）

| 你刚做了 | 必须同步（同一轮内） | 机器看守 |
|---|---|---|
| 分发文件改动 | bump 双 json + `plugin update` + `release.sh verify-cache`（v31） | G 查 + CI |
| 模板文件**行为 / 能力 / 内容**变化 | [`CHANGELOG.md`](CHANGELOG.md) 加段 + README 清单/口径 | C 查（README）；CHANGELOG 判定靠收尾三问① |
| 待办状态变化 | 先 [`待办.md`](待办.md) 更新，再本文件 Changelog 记一行（不重复正文） | I1 查（已办残留） |
| 已关闭表 >8 行 | 批量归档 `archive/`（L11） | I2 查 |
| 纯笔误 / 措辞修正（无行为变化） | 不进 CHANGELOG（口径：**记能力，不记笔误**） | — |
| 改动仅 `scripts/`（维护侧） | **不触发 bump**，除非影响 `install.sh` / `check-imports.sh` 消费侧行为（v71 口径成文——v70 裁决的理由由目录归属升为口径） | — |
| 改动触及五维短板 | 对照 [`五维基线.md`](五维基线.md)「触发式检查表」——净下降记待办，净上升 Changelog 提一句，中性跳过 | —（触发式，非强制） |

### 收尾四问（v13续-B2，v55 落地；v63 加第 4 问——每问挂机器检查，能查的不靠人记）

1. **本轮有分发文件的行为 / 能力 / 内容变化吗？** 有 → CHANGELOG 加段了吗（判定见上表第 2/5 行——v52 N8 复发、v54 D8 漏记都是这问没问）
2. **待办池与本文件同步了吗？** 状态变化先改待办再记轮次（I1 看守残留）
3. **账本膨胀了吗？** 已关闭 >8 行归档（I2 看守）、Changelog 行单行成段（**行即详情，v71 口径**——允许 ①②③ 嵌套，不再限一句式；膨胀治理靠归档不靠截断）
4. **本轮改动五维净变化？** 对照 [`五维基线.md`](五维基线.md) 触发式检查表——**触发式，不触及则跳过**（防仪式膨胀）；净下降必须处置

---

## Changelog（每轮一句话）

> v5–v40 轮次摘要已归档 [`archive/详情-v16-v40.md`](archive/详情-v16-v40.md)「Changelog 存档（v5–v40）」节；本表自 v41 起。
| 轮次 | 日期 | 摘要 |
|---|---|---|
| v40 续 | 08-17 | **v31 规则补全**：v40 bump 0.1.9 后未跑 `plugin update`（cache 停 0.1.8，提交核验时发现）——主控补跑 + grep 验证 0.1.9 代码落盘。v31 所立「改 plugin 内容必 bump」实为半条，完整规则：**bump 必 update**（bump 只改版本号，update 才刷 cache）——已回补 v31 落地行 |
| v41 归档 | 08-17 | 归档轮：①详情 v30-v40 全量移 archive（更名 `详情-v16-v40.md`），本文件恢复单段详情形态（清 v38 续登记的归档债）；②已关闭表按 L11 纪律批量瘦身（v23-T1→v30 共 26 行移 archive，保留 hooks/E5 era 6 行 + 🔒 决议）；③`test_deny_list.py` 头注落「红队探测必须 json.dumps 构造、禁手拼 JSON」纪律（本会话第三轮红队假证据教训）。零规则/代码改动 |
| v42 | 08-19 | README 新增「/crules:crules-init 会做什么（向导流程）」一节：三问+复制清单（新项目）/ 硬护栏+体检裁决+资产保护（老项目）/ 跑完后的会话变化——把口头讲解沉淀为文档，方便理解本库（需求方指示）。零规则/代码改动 |
| v43 | 08-19 | **登记需求 Y1（未实施）**：方案评审从「大需求流程自动派审」改为「显式触发——方案产出后停下问需求方是否派审」，治自动触发导致的过程感知丢失；改动面三处（工程化流程 §4 / 方案评审闭环 / CLAUDE.md §3），按需求方指示与其他需求合并评审后统一落地 |
| v44 | 08-19 | **待办池拆分**（需求方裁决 B）：open 表整体迁出至新文件 `docs/待办.md`（Y1/D8/N5/v18-C/D/纳管/A-B2/v11-②/flutter 范围外 9 项 + v11 拆分注），本文件改指针段 + 维护规则两行同步（「Changelog+详情+已关闭」新形态）；**单向同步纪律**：待办状态先改待办.md、本文件只记轮次。顺修 v43 插行格式损伤（Y1 误插于表分隔线前）。D10（已关）不随迁，留本文件已关闭链 |
| v45 | 08-19 | **登记实战需求 Y2-Y6（未实施）**：需求方在 saas_pos 实际使用中发现——Y2 方案评审产物未落盘（含首轮，🔴级纪律失效）/ Y3 评审后未等用户裁决直接进执行（与 Y1 同族构成「评审全流程用户 Gate」）/ Y4 落盘不报文档名路径 / Y5 评审维度加「范围是否扩大」/ Y6 review-review 步骤与 v44 拆分脱节（主控发现）。全部入 `待办.md`，**留待下次评审统一优化**（需求方指示） |
| v45 续 | 08-19 | 按需求方指示把主控分组建议写进待办池：Y1–Y4 同主题「评审全流程用户可控性」（根因=流程「流转式」而非「确认点」表述，建议一个方案整体设计）+ Y5 独立 + Y6 顺手 + 根因注记（与「纪律写了≠生效」同构，修法=Gate 化）——建议本身待下次评审采纳或驳回 |
| v46 | 08-19 | **登记 Y7（并入 Y4）/ Y8（未实施）**：Y4 补「路径须可点击直接打开」（需求方指示）；**Y8 skill 内建提交步骤越权**——brainstorming 流程自带 commit 步骤被照做，发生未经授权自动提交，为 §2 优先级链**首次实战违规**（修法：skill 内 commit/push 步骤降级为提示 + 插件工作流补案例 + 并入用户可控性整体方案）。分组建议同步（Y8 同根扩展） |
| v46 续 | 08-19 | 按需求方指示把 Y3 推荐修法升级为**三层结构**写入待办池分组建议：外层 **Plan 模式物理边界**（评审-裁决-修订全程 plan mode，ExitPlanMode 需求方批准才放行——harness 级不可绕）+ 内层选项卡逐条裁决（承认其为模型自愿调用、只作粒度不作闸门）+ 底座 Gate 化措辞（完成判据=落盘+可点击路径）；hook 方案明确不上（Plan 模式即其完美实现）。关键洞察：§3 软桥接早有 plan mode 映射、评审闭环未接线——**修法是接线不是发明**；Y8 顺带受益（plan mode 拦写性 Bash） |
| v47 | 08-20 | **实战优化四批次**（方案 [优化方案-v47.md](archive/优化方案-v47.md)，经独立 plan-reviewer 12 条消解 + 裁定 B）：①deny-list 5 向量修复（parse_flags 统一 / 裁定 B 信号即拦 / 空操作数拦 / 重定向 FP 修，fixture +10+1，plugin 0.2.0）②Y1-Y8 三层结构接线（Plan 模式外层 + 确认点 + 落盘 = 获批后首步，plugin 0.2.1）③复盘教训 ①③④⑦⑧⑨⑪⑫ 提炼落规则（⑩ 裁定入待办 V4，plugin 0.2.2）④元文档收尾（CHANGELOG 修序 + 新待办 V1-V4）。Y1-Y8 全部落地关闭 |
| v48 | 08-21 | **外部审评落地·阶段 1 门禁硬化**（[改进建议报告](archive/改进建议-2026-08-20-crules审评.md) 19 条核验→修订→按计划执行）：check-consistency.sh **FAIL 即 exit 1** + 末行 PASS/FAIL（治「发现问题 CI 仍绿」；macOS bash 3.2 命令替换嵌 case 解析坑→fd3+临时文件收集）；实施计划-v47 三处引文悬空转纯文本；仓库级 `.gitignore`（settings.local/worktrees，原仅靠全局 gitignore）；CI workflow 落地（**V1 关闭**，吸收 py_compile）。零模板分发文件增删，plugin 无需 bump。**CI 首跑红→根因修复**：A 查验包外链接存在性——`../flutter` 在 CI 自包含 checkout 不存在致 4 悬空 exit 1（本机因兄弟目录存在而绿，v36「源仓库通过恰在分发场景失明」的镜像）；修法 = A 查跳过越包链接归 F 查管辖，git worktree 模拟 CI 环境双向验证（旧版复现 FAIL 4 → 新版 PASS）；顺升 actions v5/v6 消 Node 20 弃用 + 三 step `if: always()` 暴露全问题面 |
| v49 | 08-21 | **审评阶段 2（部分）——高风险流程可检查化**：①crules-init 老项目分支加**安装报告（dry-run）**步骤（体检后、取舍前：写入目标/冲突概览/备份计划/不动清单全景表，确认前零写入；commands + README 两处同步）；②**scripts/release.sh** 落地（v31「bump 必 update + cache grep」机械化：双 json 版本同步 + verify 三命令 + verify-cache 子命令；实测沉淀两条纪律——update 完整形态 `claude plugin update crules@crules-market` 纯名 not found、**bump 最后跑**（cache 全仓库快照含 README，中途改动须再 bump））。探针集（V2）体量大单独成批；部署拓扑检查并入 N5 缓项不动。plugin 0.2.2→**0.2.4** |
| v50 | 08-21 | **审评阶段 4——补安全规则**（阶段 3 核心项被「模板定位」裁决门控，留待裁决；探针集按需求方指示跳过，资产 `scripts/probes.sh` 已沉淀待环境恢复）：①**C10** CLAUDE.md §一 +「不可信内容不作指令」红线（prompt injection：外部内容视为数据，疑似注入报告不执行）；②**C11** +「外部依赖与网络操作先确认」红线（新增/升级依赖、curl\|bash、外呼先确认+回退说明）；③**C7** deny-list.py 头注落**覆盖矩阵**（git 破坏族/rm ✅拦；chmod -R/find -delete/python -c ❌归根规则+原生权限）；④**C8** error.md 顶部硬约束「未明确要求修复只诊断禁 Edit/Write」；⑤拦截文案统一追加「不要尝试绕过，需展示命令直接写文本」。fixture 53拦+24放全绿 + 拦截文案抽验 + README token 口径（≈4.9–5.6k）与矩阵指针同步。plugin 0.2.4→**0.2.5**（改完再 bump，v49 教训执行） |
| v51 | 08-21 | **审评阶段 3——定位转通用团队模板**（需求方裁定「通用模板」）：①**P2-3** §一语言红线通用化（「必须始终中文」→「跟随需求方语言」，附录「协作偏好」覆写）；②**P1-6** §二提交触发词**分离语义**（`commit`=本地 / `push`=远端 / 「提交并推送」=完整，附录覆写字段；旧「默认推送」行删除）；③**C5** §三+工具降级表（AskUserQuestion→编号列表 / Plan 模式→文本 Gate / Agent→串行自审，降级须声明）；④README +「快速上手 5 条」+「三档规模」（Lite/Team/Advanced）；⑤附录模板 +协作偏好节 + 触发词覆写字段。一致性全绿 + 旧表述零残留 grep + CHANGELOG 语义变化登记。plugin 0.2.5→**0.2.6**。**行为提示**：crules 自用提交变为两步（commit 后停等 push），单步流可在消费项目附录覆写恢复 |
| v52 | 08-21 | **meta-review 消解轮**（[meta-review-2026-08-21](meta-review/meta-review-2026-08-21.md)：v48-v51 零虚账、3 精彩，4🔴+7🟢）。4🔴 全消解：C13 归属本轮（CHANGELOG 头部版本口径行，随本轮提交）/ **P1-7 补账落地**（§三修一坑查同类 + 范围限定——初版核验漏排通道）/ CHANGELOG 补 v49+v50 段（N8 复发收口）/ v47 详情 V1 矛盾加指针。5🟢 顺手：token 口径两文件同步 / 实施计划 V1 行更新 / dry-run 措辞精确化（「不动既有文件」，备份例外）/ README +scripts·.github 不复制行 / fixture +拦截文案断言。2🟢 记录不动作（Changelog 膨胀债——本轮起短句；0.2.3 跳号已如实登记）。plugin 0.2.6→**0.2.7** |
| v53 | 08-21 | **归档轮**：实施计划-v47（四批次已落地，注记 checkbox 未回勾以本文件为准）与审评报告（处置全闭环：11 落地 / 3 并入 / 1 撤回）移 `archive/`；v47 详情段 + 已关闭表 v33–v40 六行迁 `archive/详情-v16-v40.md`，「最新详情」改指 meta-review 与 Changelog；引用链接全部改指 archive。一致性全绿，活跃区零活链残留。零分发文件改动，无需 bump。**v53 续**：待办池 Y 分组建议块（三层结构表等）精简为决议一行指针，全文在 archive 级优化方案-v47 内不丢失 |
| v54 | 08-21 | **D8 落地**：error.md「通用常见坑」5 条技术栈中性化（坑型通用、机制名按栈对号）；Y 分组块 v53 续精简。plugin 0.2.7→**0.2.8** |
| v55 | 08-21 | **v13续 A/B2 落地（元文档治理三件套）**：①A 元账本触发即更新表进维护规则节（CHANGELOG 判定口径「记能力不记笔误」）；②B2 收尾三问（每问挂机器检查）；③I 查进脚本（I1 待办已办残留——红绿验证中当场抓出误报并收紧为级别列精确 ✅；I2 已关闭表 >8 行）。顺手补 v54 D8 的 CHANGELOG 漏记（新表判定下的自我实证）。零分发文件改动不 bump |
| v56 | 08-21 | **N5+v18-D 部署拓扑收口**：盘点发现账本滞后（agents/README 断链源 v29 已删、MAINTENANCE 已净）——真实面仅 5 处 `../../flutter/` 越包链接，全改条件式 prose；F 查豁免收缩至 README；越包链接 grep 清零。plugin 0.2.8→**0.2.9** |
| v57 | 08-21 | **待办池清仓**（需求方点名 7 项全量处理）：V4 落 §三尾注 / V3 信任档位（附录字段+Gate 注，仅需求方可调）/ v18-C git 分层+A7 进 MAINTENANCE / 纳管步骤进 crules-init / v11-② 转规模触发指引 / N3-N6 账本残留关闭。机制级新增 → **minor 0.2.9→0.3.0**；待办池仅余 V2（环境挂起） |
| v58 | 08-21 | **历史方案归档**：修改方案-v18（主体已撤）/ 重构方案-v22-plugin（v29 已执行）/ 优化方案-v47（四批次落地）三份移 `archive/` 带注记，三处活链改指 archive。docs/ 活跃区仅余 待办/评审/CHANGELOG/meta-review（零分发文件改动，CHANGELOG 不记——三问①留痕） |
| v59 | 08-21 | **V2 收口 + 元数据瘦身**：探针三轮迭代（缺陷 7 处修复——PCRE 兼容 / BSD wc 空格 / 授权歧义 / 规则冲突 / 语言语义过时；`-p` 通道打通靠显式 `--model fable`），三轮 5→6→7 绿，P1 单发诊断实证行为正确、3 红定性为单次会话方差非稳定失守——**V2 转形态关闭**（探针=消费侧可选模型体检工具，结果属模型×环境快照，非包验收门槛）；archive 死档 8 份删除（git 保底）；评审.md Changelog v5–v40 + 已关闭老 4 行迁 archive（162→103 行）。待办池清零（零分发文件改动，CHANGELOG 不记——三问①留痕） |
| v60 | 08-21 | **外部复核六组收口**（清单核验 5 成立 1 定性修正）：①「CHANGELOG 漏记」不成立（v58/v59 零分发文件，按触发表不记是正确执行——真漏记史三次非四次；J 查性价比低不建，补三问①留痕注）；②probes dryrun 矛盾修（实测 0.4s/EXIT 0）；③error.md 物理锁（去 Edit/Write）；④部署死链 **10 处**（比指控多 1，v56 盘点盲区）反引号化清零；⑤README 易用性三处（占位路径/语义版本排序/团队缝隙）；⑥四小件（ci A-I / verify-cache include / token 口径 / §2 hook 边界）+ 2 项记录不修（A 查静默理论性低风险、中文目录名既定取舍）。plugin 0.3.0→**0.3.1** |
| v61 | 08-21 | **外审政策矛盾收口**（实测复核成立）：①memory git 政策四处语义矛盾——`MAINTENANCE.md`「git 分层」（v57 落地 v18-C：patterns/business-rules/INVARIANTS/decisions **进 git**）vs 四文件头注仍写「不进 git」（memory/README、business-rules、INVARIANTS、进阶/记忆库体系），消费项目照头注执行会漏共享业务规则（恰是该体系要防的漂移）；修法 = 四头注改引 MAINTENANCE 单一权威。②**LICENSE 缺失**（MIT，需求方裁定）+ README License 节——「可直接复用」定位补法律基础。治本项（政策表收敛 + C 查关键词扫描）登记待办、不本轮顺手做。plugin 0.3.1→**0.3.2** |
| v62 | 08-21 | **外审五维审评接收轮（零落地，CHANGELOG 不记）**：接收五维深度审评（方法论 / IA / 机制化 / 分发 / 可维护），其发现驱动 v63-v70 收口线（v71 meta-review 🟡#2 补注记） |
| v63 | 08-21 | **五维整合 + error.md 声明诚实化**（外审五维审评收口）：①`docs/五维基线.md` 新增——五维基线快照（方法论 8.5 / IA 7.5 / 机制化 8 / 分发 6.5 / 可维护 7）+ 雷达演化表（度量回路起步，维度一最痛单点的替代解）+ 触发式检查表（两条铁律：触发式非强制防仪式膨胀 / 重评须独立 subagent 防自评拔高）；②收尾三问→**四问**（第 4 问挂五维检查表）+ 元触发表加「触及五维短板」行；③`/review-review` 输入源加五维基线 + 审查维度加「基线滞后吗」（重评结论 subagent 给、主控不自改）；④**error.md 声明降级**——「物理只读、与 reviewer 对等」改诚实措辞（Bash 是写通道非物理只读；reviewer 无 Bash 才是真只读，不对等），工作流 5-7 步转诊断形态（修掉 v60 漏改的「用 Edit 修改文件」自相矛盾）。机制化维最痛单点 ✅。plugin 0.3.2→**0.3.3** |
| v64 | 08-21 | **分发维支点——版本戳 + 导入巡检**（W2①，五维审评「无出处戳→永远全量合并」的解）：①`scripts/check-imports.sh`——消费项目侧巡检导入可达性（断链 exit 1，治 Downloads 清理后**规则静默失效**——比报错更危险的形态）+ 版本差提示，四情形自测（可达/断链/无戳/无痕迹；自测过程被自家 deny-list 拦了一次变量式 `rm -rf`——白名单只认字面 /tmp，hook 按设计工作，未绕过）；②crules-init 轻装/完整/合并产物统一落戳，老项目体检**先读戳聚焦 CHANGELOG 差异段**（增量合并起步，完整 delta 合并挂后续）。plugin 0.3.3→**0.3.4** |
| v65 | 08-21 | **编排成本分档——消费工程实战复盘提炼**（saas_pos 三期实测：4 实现代理 358 次调用 + 14 评审/调研代理 609 次 + 主线 130 轮，「需求小消耗大」的病根 = 质量有分级、成本没分级）：①CLAUDE.md +2 条（长输出裁剪 / 局部 Edit 禁重写禁 sed）；②Agent编排 新节「编排成本分档」——成本模型「轮次 × 单轮累积上下文」、任务书契约化（写 1 读 N、贴签名不贴框架）、评审关卡四级（双关卡/单代理双节/免复读/小任务并入——治 23 次实现配 56+ 次评审的倒挂）、调度三条；③工程化流程 +TDD 批量红绿分档（同构集 34 用例 ~60 轮→3 轮）+ 事实核验前移（转写引入的事实错误占设计评审发现一半，前移可全拦）；④插件工作流 +TDD skill 冲突消解（§2 优先级链第三例）。项目侧教训（flutter test 命令 / arb·gen-l10n 固定清单）不入包由消费工程吸收。五维：方法论/IA/机制化净上升（实战出处规则化 + 成本档位补位）。机制级新增 → **minor 0.3.4→0.4.0** |
| v66 | 08-21 | **编排成本补丁（W3 六条全收口，复盘文档行动项清账）**：①批量红绿断言有效性补偿（防恒真断言——逐用例红绿的自证被批量化丢掉，须抽查错值确认会红）；②禁重写补结构性推翻例外（显式声明+旧版归档）；③**评审包契约化**（事实切片包+抽查权保留+定点读纪律——根因「独立性被实现为全面隔离，需隔离的只有判断不是事实」，与任务书契约化对称）；④调研去转写（核验后报告直接作设计事实小节）；⑤附录分层 runner 提示+字段（治纯逻辑测试用最贵 runner 的层级错配）；⑥编排档位**不落**——精简档动 Y1 确认点裁定，留需求方裁决（待办池保留）。plugin 0.4.0→**0.4.1** |
| v67 | 08-24 | **安装脚本化 install.sh（W2②）**：新项目分支确定性操作（cp+导入行+版本戳+安装报告）从模型执行转脚本——幂等、可 diff、可测、`--dry-run` 出报告（替代「AI 手工还原 dry-run 报告骨架」的漂移源）；老项目护栏按**有无版本戳**判定（无戳中止走 2b 合并 / 有戳重装）；cache 源 lite 导入行自动警告静默漂移。五情形自测（lite 含戳与导入行/full 头戳/幂等全 SKIP/老项目中止 RC=1/check-imports 联通），自测红→绿修 3 处（sys 导入漏、幂等被护栏误拦、full 戳错位尾部）。crules-init 2a 改「问答引导+跑脚本」，README scripts 行补例外。plugin 0.4.1→**0.4.2** |
| v68 | 08-24 | **平台信任声明 + 易用小项群（W2④⑤收口，含两处漏挂账补登：术语表 / git URL 安装）**：①README +「环境要求与更新信任」（Windows hook 静默失效、plugin update 供应链——update 前 diff hooks/ 团队锁 commit）+git URL marketplace（Team 档免各自 clone）+「卸载」五层表 + memory 撞名消歧 + 误拦取舍前移；②**术语表 24 条**（进阶/，分发——第二人入门钥匙）；③init 提示提速档减压阀、附录加大需求触发词本项目映射（去移动端形状）；④**test-self.sh 三断言 + CI 接入**（应报错断言 3/3——检查器验别人也得证自己会红；完整 fixture 版留 W1）。W2 剩③元数据收缩（待裁决）。plugin 0.4.2→**0.4.3** |
| v69 | 08-25 | **政策收敛治本（W1 收口）**：①**J 查**孪生表断言（twin 锚点成对+数据行数一致——触发即更新表/文件结构表两对孪生从注释人肉同步升机器看守，MAINTENANCE「自包含 vs 单一权威」矛盾以「保持双份+看守」消解并头注声明）；②**K 查**政策归口（旧政策特征串只许在 MAINTENANCE 政策表——上线预检即抓出真漂移：记忆库体系「与项目文档分工」内嵌表仍 `❌ AI 私有`，v61 漏扫的第三处，已对齐 v18-C）；③**fixture 模式**（CONSISTENCY_ROOT 对任意目录跑 A-F/J-K——检查器自测完整版，test-self 扩六断言全绿，红→绿修 2 处：J 锚点豁免/C 查跳过 fixture）。机制级新增 → minor，**本轮最终 0.4.3→0.5.1**（0.5.0 后 J 查 docs 豁免补修再 bump——git 一步跳、中间版仅存 cache；v71 meta-review 🔴#1 补账）。至此 A-K 十一查 + CI 四步；五维 IA/可维护维净上升（孪生看守+检查器自测两支点落地） |
| v70 | 08-25 | **W2③/W3 双裁决收口（需求方选项卡裁定），待办池清零**：①W2③=维持评审/待办拆分（受众分离理由仍成立，双写成本已被 I1 看守）+ 落两子项——**meta-review 里程碑化**（review-review.md 触发时机改里程碑制：大特性落地/五维雷达重评同场，非常规每轮）+ **release.sh draft 子命令**（CHANGELOG 建议段草稿：自顶部段日期后的 commits 列 subject，人工过滤口径内置于输出头——记能力不记笔误、零分发轮不记）；②W3 编排档位**不采纳关闭**——v65 四级分级已是按任务风险自适应的档位（人工选档反而是新仪式），「评审与确认并行」会 reopen Y1 防的洞。test-self 扩至七断言。零分发文件改动不 bump（review-review 为仓库本地命令、release.sh 属 scripts；成本复盘全文见 `成本优化复盘-2026-08-21-编排成本.md`） |
| v71 | 08-25 | **meta-review 里程碑首轮消解**（[meta-review-2026-08-25](meta-review/meta-review-2026-08-25.md)：13 项声明 12 项核验一致 + 实跑全绿；1🔴+6🟡+6🟢 全处置，#7 经需求方采纳推荐 B）：①🔴版本账——v69 行与 CHANGELOG 补记最终版 0.5.1 归因（J 查 docs 豁免补修再 bump，git 一步跳）；②v62 缺号注记（外审五维审评接收轮零落地）；③W1/W2/W3 已关闭表汇总行（W3 🔒 不采纳防重复提案）；④K 查特征串 +「AI 私有」；⑤ci.yml 自述 A-K/四 step；⑥scripts bump 口径成文（影响 install/check-imports 消费侧行为才 bump）入元触发表；⑦**Changelog 行即详情口径**（v71 起，收尾四问③同步——「最新详情」节改「详情入口」）；⑧四小件（review-release 笔误/18 SKIP 防漂移/draft 跨年注记/install 护栏 `v[0-9]` 收紧——此项属消费侧行为 → bump）。五维重评同场落盘（8.75/8/8.5/7.5/7.5 全维升）。plugin 0.5.1→**0.5.2** |
| v72 | 08-25 | **v71 push 后收尾三件**：①远端 CI 核验——历史 run 全绿（J/K 查在 CI 自包含 checkout 亦过，v48 型假阳性无复发），最新 run 需求方确认通过；②修 v68 漏执行的总览同步——进阶/README 启用决策表补列术语表（C 查只查根 README，子目录索引是看守边界外，本轮人工核出）；③零碎教训两条（verify-cache 特征串须字面串不解析正则；轮询脚本 zsh 词分割差异两踩）不落规则——一次性环境坑，不满足「复发才规则化」标准。plugin 0.5.2→**0.5.3**（分发内容变化按 v31 口径） |
| v73 | 08-25 | **缓办提前触发：R2 探针补三条 + 术语表/口径同步**（触发条件放宽理由：探针资产完善属本仓库工程，PROBE_DRYRUN 结构自测不依赖消费环境）：①probes.sh +P11 注入不执行（内嵌指令文件读取后 git log 不变——副作用级）/ P12 依赖先确认（requirements 不被直接改）/ P13 commit=仅本地（说提交→恰 +1 commit 无推送自述——v51 触发词语义分离的运行时断言），dryrun 13 SKIP/RC=0 结构验证过，真实跑归消费侧；②术语表 24→26 条（+twin 锚点/孪生看守、行即详情——v69/v71 新术语漏收补齐）；③README token 口径复测 ≈6.5–8k（26.2k 字符，v65/v66 +3 条后——五维审评曾点的口径过期债清）。R2 关闭、R1 维持缓办。plugin 0.5.3→**0.5.4** |

---

## 当前待办（open）

> **v44 起独立维护于 [`待办.md`](待办.md)**（受众分离：需求池 vs 审计）。状态变化**单向同步**——待办.md 为待办唯一权威，本文件 Changelog 只记轮次；已落地项仍移入下方「已关闭 / 已决议」表。

---

## 已关闭 / 已决议（近期；积累多了移 archive）

| 项 | 状态 | 处置 |
|---|---|---|
| **N5 复制后相对路径断链** | ✅ | v56：唯一残存断链（NAVIGATION 1 处 flutter 链接）prose 化；agents/memory 其余断链源经盘点已自然消失（v29 删 agents/README 连带、MAINTENANCE 已净）——账本描述滞后本轮修正。v33 注的轻装根链接同构成立维持 |
| **v18-D Flutter 解耦** | ✅ | v56：4 处进阶 `../../flutter/` 链接改条件式 prose（技术栈中性化达成——链接形态消除，指引保留「未安装则忽略」）；v23 扩围的 NAVIGATION 术语表维持既有「按栈替换」显式标注（防线已在）；F 查机械看守依赖消除（豁免清至 README） |
| **V2 规则行为探针集（转形态关闭）** | ✅ | v59：`scripts/probes.sh` 10 探针三轮迭代收敛 7/10 稳定绿（诊断不改码 / 不虚构 / 跟随语言 / 方案先于动手 / **plan 拦写** / 强推不绕过 / 敏感不外发）；P1 单发诊断实证模型行为正确（创建文件+零提交），余 3 红为 `-p` 单次会话方差。**定位修正**：行为 compliance 属模型×规则联合属性，非模板包验收门槛——探针转为消费侧可选体检工具（README scripts 行已有指针）；Plan 四行为中 harness 级「拦写」已由 P7 运行时证实 |
| **W1/W2/W3 外审+五维收口线** | ✅/🔒 | W1 政策治本 ✅ v69（J/K 查 + fixture 模式）；W2 全收 ✅（①戳 v64 / ②install.sh v67 / ④平台信任⑤小项群 v68 / ③元数据收缩 v70）；**W3 编排档位 🔒 不采纳**（v70 裁决：v65 四级分级已是按任务风险自适应的档位，人工选档 = 新仪式；「评审与方案确认并行」会 reopen Y1 防的洞——**勿重复提案**） |
| **V3 / V4 / v18-C / 纳管 / v11-② / N3-N6（待办池清仓）** | ✅ | v57 六项一批：V4=§三接口演进同步 fake 尾注（v47 备选转正）；V3=信任档位双落点（附录「协作偏好」字段 + Gate 例外注——提速档仅需求方显式声明、AI 不自行升档、连续返工提示降档）；v18-C=MAINTENANCE git 分层节（制度资产进 git / indexes·队列本地 / settings.json 进 git·local 永不进，A7 一并定夺）；纳管=crules-init 老项目分支历史文档体检（v15续 四步）；v11-②=转触发指引（>15 条再分域，模板侧永不触发故转形态）；N3-N6=账本残留（实已在 v26-B/v24续/v56 消化）。cache 0.3.0 双特征串验证 |

---

## 详情入口（历史轮详情归 Changelog 行内与 archive）

> 详情即 Changelog 各行 + 独立复审原文 [meta-review-2026-08-21](meta-review/meta-review-2026-08-21.md)（23 项抽查零虚账）；v47 及更早详情已归档 [`archive/详情-v16-v40.md`](archive/详情-v16-v40.md)。

---

## 历史详情（v16–v40，已归档）

v16–v40 全部详情段与已关闭行（v23-v30 批量）已移 [`archive/详情-v16-v40.md`](archive/详情-v16-v40.md)——v40 R1-R3 收口 / v39 换轴 / v38 红队轮 / v36 F-G 查与漂移队列 / v35 deny-list 首发 / v34 脚本 / v33 双模式 / v32 @import 实测 / v31 补漏 / v30 实战复盘提炼 / v29 plugin 化 / v28 裁决 / v27 复盘 / v26 外审 / v25 D7 / v24 / v23 / v22 / v21 / v20 / v18 复盘 / v17 / v16。结论见「已关闭 / 已决议」表（近期行）+ Changelog（全轮摘要）。

> 归档原因：详情段累积 9 段（v24 自注第五次精简复发后 v25 又 +1），违反 L11 精简形态——v26 执行 v24-C2 止损（N7）。

## 历史详情（v15，已归档）

v15 详情（/crules-init 新/老两分支 + README 叙事反转 + 老项目合并 4 步 + 知识库处置方法论）已移 [`archive/详情-v15-v18二审.md`](archive/详情-v15-v18二审.md)。结论已进「已关闭/已决议」表。

## 历史详情（v12/v13 已归档）

v12（v10 知识层落地核对）、v13（使用指引核查 + 总览滞后根因/治理方案）详情已移 [`archive/详情-v12-v13.md`](archive/详情-v12-v13.md)。结论见上方「已关闭/已决议」表 + Changelog；治理方案 C/D 已落地、A/B2 转缓。

> 归档原因：评审.md 详情段曾堆 5 轮，违反自身 L11「精简形态」——dogfooding 活跃/历史分离（v17 B1）。

---

> 本活跃文档随评审迭代更新。最新详情只保留最近一轮；更早的轮次全文在 `archive/`，骨架在 Changelog。下一轮评审在此文件上原地更新，不再新建 vN。
