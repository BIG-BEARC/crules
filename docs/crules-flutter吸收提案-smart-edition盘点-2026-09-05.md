# crules-flutter 吸收提案:saas_pos_smart_edition 全量盘点(2026-09-05)

> 性质:提案文档,待评审——与既有优化提案合并评审,未评审前不动 crules-flutter / crules 任何文件。
> 来源:brainstorming + 对 smart_edition 工程文档的全量盘点(逐文件实读,非印象)。
> 盘点对象:`saas-suite/03-code/src/app/saas-cashier/projects/saas_pos_smart_edition` 的 CLAUDE.md(862 行)、docs/(9 篇)、doc/(40+ 篇)、tool/quality(8 脚本)、pitfalls.md(14 条红线)。
> **目标口径**(需求方原话):开启新 Flutter 工程时有模板可依,并加速老项目重构。

## 一、结论先行

smart_edition 的 CLAUDE.md 本身已是 crules 方法论的产物(§一协作红线即 crules 裁剪版),不是待提炼的原始矿。本轮可收增量:

- **crules-flutter 直接吸收**:3 强 + 2 降档,共 5 条(§三)
- **转 crules 主仓议题**:5 条跨技术栈(§四)
- **结构决策待需求方拍板**:工程脚手架是否入 crules-flutter(§五)
- **不收**:通用教程类、项目特有类(§六)

## 二、pitfalls 14 条红线逐条判定(pitfalls.md 实读)

| 条 | 内容 | 判定 |
|---|---|---|
| A1 | 指着代码问「为什么还有」=编码模式问题,不是实例问题 | 中;协作语义解读,暂不收(偏会话技巧,等更多实证) |
| A2 | 「一起改了」默认限当前功能域 | **已覆盖**(crules 范围边界 + P1-7) |
| A3 | 明确祈使句直接执行 | **已覆盖**(Gate 例外) |
| B4 | 值域码走常量类 + 取值对照源码核验 | 残差「常量取值先核验后落笔」→ 桶 2 主仓 |
| B5 | 平行字面量表配锁定测试 | **桶 2 主仓**——本质语言无关(平行结构任何栈都有),Flutter 只是案例背景(初判放 crules-flutter 系误判,已修正) |
| B13 | 值域清零按值反查,不按语法形态正查 | **桶 2 主仓**——三期连续漏网实证,方法论语言无关 |
| C6 | 脚本批量改码幂等 + 清零扫描收尾 | **桶 2 主仓** |
| C7 | 结构化文件程序化编辑后必过解析校验 | **桶 2 主仓** |
| C8 | 批量编辑收尾 analyze+test | **已覆盖**(分级验证) |
| C14 | 门禁退出码显式验证,禁 `cmd \| tail` 后当通过 | **桶 2 主仓**——zsh 无 pipefail,shell 通用坑 |
| D9 | 新顶层类名先全局 grep 撞名 | 桶 1 降档——单命名空间语言通用,勉强可留 Flutter,标注边界 |
| D10 | DTO 字段名逐字抄在线解析处,不凭语义猜 | **桶 1 强收**——两起实战,猜错=静默 null 不报错,Dart 解析特有失败形态 |
| E11 | 子模块 git 输出依赖 cwd,交叉验证 | **已覆盖**(crules 跨仓绝对路径/实战 6 踩);`git ls-files --error-unmatch` 具体命令为小增量 |
| E12 | check-ignore 对已跟踪文件不生效 | 不收——通用 git 知识,不占模板体积 |

根因归纳(指令理解停在字面/中间产物当结果/猜测替代查证)不单独收条——crules 各红线已覆盖,形态比逐条更稳。

## 三、桶 1:建议收 crules-flutter(Flutter 特有形态)

| # | 候选 | 来源 | 判定与形态 |
|---|---|---|---|
| 1 | **DTO 字段名以解析代码为准逐字抄,不凭语义猜** | pitfalls D10 | 强收,§四/8.1;注明失败形态=静默 null |
| 2 | **主题扩展类加字段四处同步(构造/factory/copyWith/lerp)+ widget 测试必注入主题工厂(否则 `appColorTheme!` null 直接崩)** | CLAUDE.md 规则4 / docs/theme-guide.md | 强收,§八新增小节;「改 A 必同步 B」清单 + 崩溃坑,双预设适用 |
| 3 | **多主题项目级声明替代逐次询问** | CLAUDE.md 规则4 改造 | 收,但**改形态**:项目附录一次性声明「本项目是否多主题」,布局改动按声明走,声明缺省才问——一次声明替代 N 次询问(初判「每次布局先问」形态系噪音,已修正) |
| 4 | 新顶层类名先 grep 撞名 | pitfalls D9 | 降档收——标注「单命名空间语言通用,Flutter/Dart 命名空间特有代价(撞名=编译错+改两遍引用)」 |
| 5 | 响应解析归模型,API 层禁解析 | CLAUDE.md | **缓收**——非 Flutter 共识(不少工程刻意放 API 层),直接进 8.1 全栈通用 = 批 3a 风险 E 翻版;降级为挂预设 A 块下,或等第二个项目实证 |

吸收时逐条做适用面判定,记录入 fork-coverage(沿批 3a 惯例)。

## 四、桶 2:转 crules 主仓议题(跨技术栈,本轮不混入)

| 候选 | 理由 |
|---|---|
| 值域清零按值反查(B13) | 语法形态枚举不全,唯一判据=对值本身 grep 清零;三期连续漏网实证 |
| 平行数据结构必配覆盖锁定测试(B5) | 语言无关;smart_edition 实战当场抓回归 |
| 管道退出码陷阱(C14) | `cmd \| tail` 当通过、`exit=$?` 显式验证;shell 通用 |
| 脚本批量编辑幂等+解析校验收尾(C6/C7) | 语言无关 |
| 常量取值先核验后落笔(B4 残差) | 「不硬编码」的取值流程增量 |

## 五、桶 3:结构决策待拍板——工程脚手架的去向

候选内容:tool/quality 门禁脚本套件(8 脚本)、QUALITY.md、pitfalls.md 三合一形态(红线速览+案例档案+提交前自查清单)作为坑卡模板、scripts/add_file_header.sh。

矛盾:crules-flutter 现为**规则库**(CLAUDE.md + agents + hooks);脚手架收进来即成「规则+脚手架」混合体。

- 方案 A:入 crules-flutter——对「新工程拷走即用」目标最直接;但仓库性质改变,规则与工具耦合发版
- 方案 B:独立 starter 仓库——性质干净,但需求方开新工程要拼两个来源
- 注意:quality 脚本套件自身踩过 C14 管道退出码坑,入模板前须先修(单独跑+显式 exit)

## 六、不收(记账)

- **doc/ 通用库教程**(GoRouter/Dio/SQLite/ScreenUtil 使用指南等):AI 已知的公开用法,零增量——与 Clean Code 整包不吸收同构:只收踩出来的约定,不收 AI 已知常识
- **项目特有**:×1.265 跨设计稿换算系数(可通用残差「跨坐标系须声明换算系数禁照抄」因触发场景低频,暂不收)、CartCommand、算价服务、离线订单生命周期、字段对照文档、主副收银 WebSocket、OverlayPanel 互斥——归 smart_edition 项目附录/memory
- **外部资产**:`.wolf/`(同事 OpenWolf 记忆库)只读,不收
- **pitfalls 附带发现的悬空引用**(工程 CLAUDE.md 引用的 check_inline_textstyle.py 不存在、.wolf 路径解析不到):属 smart_edition 自身待修项,已在其文档记录,不在本轮范围

## 七、执行前置条件(评审通过后)

1. 桶 1 各条:先核 crules-flutter app 模板现行原文无等价条目,再局部 Edit 插入(禁整文件重写),适用面判定随收随记 fork-coverage
2. 桶 2:另开 crules 主仓议题,按主仓残差吸收流程走,不搭车
3. 桶 3:需求方定方案 A/B 后再动
4. 本轮全程不动 smart_edition 工程任何文件(只读盘点)
