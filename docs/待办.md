# crules · 待办需求池

> **单一权威**：待办项（历轮评审遗留 + **需求方直接提出的优化需求**）的唯一记录处。
> v44 从 [`评审.md`](评审.md) 拆出——受众分离：本文件面向「要做什么」（需求方视角的行动池），评审.md 专注「发生了什么」（审计链）。
>
> **同步纪律（单向，防派生漂移）**：待办状态变化先在本文件更新，再到 [`评审.md`](评审.md) Changelog 记一行（评审只记轮次，**不重复待办正文**——避免双写不一致）。
> 符号：🔴 重要 / 🟡 中等 / 🟢 小优化；✅ 已落地（移 [评审.md](评审.md) 已关闭表）；🔒 已决议锁定；**缓** = 暂缓（等触发条件）

---

## 当前待办

| 项 | 级别 | 说明 |
|---|---|---|
| **V2 规则行为探针集** | 🟡 | 约 10 个 `claude -p` 探针断言核心红线（未说 commit 不提交 / 粘贴报错不改码 / 「直接改」不出范围 / 问不存在文件承认查无）；验收项含 Plan 模式接线四行为：派审走 Plan 模式、ExitPlanMode 后才落盘、写性 Bash 在 Plan 模式内被拦、docs/ 写入被拦（v47 批次二静态交付的运行时补验） |
| **V3 Gate 信任分级设计** | 🟡 缓 | 连续 N 任务零返工 → 需求方显式调档扩大极简范围（治 gate 疲劳橡皮图章）；属新机制，单独 brainstorm |
| **V4 接口演进同步测试 fake（教训⑩）** | 🟢 | 项目侧约定（v47 裁定不入本包通用规则），消费工程吸收：改接口 / 签名必须同步其测试与 mock/fake 并跑全量——实战第二次出现（状态图复盘合并教训 3） |
| **D8 error.md 通用坑=响应式框架默认值** | 🟡 | `agents/error.md`「通用常见坑」5 条全是响应式/移动端默认值（build 副作用死循环 / watch 丢字段 / 热重载残留）——v18-D「通用正文移动端默认值」同类漏网，并入其复盘清单一并扫（同类现存：error.md / NAVIGATION 术语表；§4 一处 v23-T1 已修）。**v30 注**：本轮新增的「根因定位方法论」节已按通用形态撰写，D8 仅涉旧「通用常见坑」节 |
| **N5 复制后相对路径断链（v18-D 扩围）** | 🟡 | agents/、memory/ 复制后下沉 `.claude/`，其指向根级文件的相对链接（agents/README 的 `../进阶/`、`../CLAUDE.md`，NAVIGATION/MAINTENANCE 的 `../../flutter/`）全部偏移一级——与 N4 同类但范围更大（**crules 原位拓扑 ≠ 部署拓扑**）。v18-D 复盘应按「复制映射拓扑」整体设计（如 agents/memory 内统一 prose 式「项目根 CLAUDE.md」引用），非逐条补路径。**v33 注**：轻装模式下根 CLAUDE.md 内指向附录/进阶的相对链接在消费侧反而成立（同构复制），agents/memory 断链仍在 |
| **v18-D Flutter 解耦**（暂缓） | 🟡 缓 | 4 处 `../../flutter/checklist.md` 死链改技术栈中性化。**暂缓**：牵连"通用包定位"+"多规模 Flutter 项目"，需单独 brainstorm，不顺手做。待多规模 Flutter 方案启动一并。**v23 补充**：应扩范围——除 checklist 死链外，「通用正文里的内部移动端默认值」同类（§4 证据尺度 v23-T1 已修一处）；复盘时一并扫 CLAUDE.md/进阶 其余移动端默认值。**v24续-N4 措辞修正**：v23 已实测这 4 处在 crules 原位是**活链**，非死链——真实问题=**复制到消费项目后断链**（相对路径逃出 ai-code）；两句话治法不同，复盘设计按此理解。**v26-N5 扩围**：断链不止 flutter 链接——agents/、memory/ 下沉 `.claude/` 后，所有指向根级文件的相对链接（`../进阶/` 等）同样偏移一级，应按「复制映射拓扑」整体设计 |
| **v18-C memory git 分层**（暂缓） | 🟡 缓 | memory 分两类（制度资产进 git 团队共享 / indexes 本地）。**暂缓**：当前"全本地"先用稳，凭痛感重启。A7（settings.json 进 git）随本项一并定 |
| **老项目历史文档分级纳管**（原 v16-②④） | 🟡 缓 | v15 续方法论已落评审文档，但**未作为 crules-init 老项目分支显式步骤**。需设计：分级(时效/沉淀/过程)→信任分级(L0 快照)→纳管/隔离→迁移即验证。属设计新步骤，与 v13续 A/B2 一并 brainstorm |
| **v13 续 A/B2 元文档治理（未落地）** | 🟡 缓 | C（总览同步检查→评审.md L14 ✅）+ D（CHANGELOG.md ✅）已落地；**A（MAINTENANCE 元触发表）+ B2（自检 checklist）未落地**——v16 证明光靠 L14 不够。但 A/B2 是**设计新机制**（不是填空），应单独 brainstorming 一轮，不顺手做。**v23 E6 升级**：评审.md 精简纪律第四次复发（仍 6 段详情），正是 A/B2 未落地的代价——优先级应上调，不宜再缓。**v24-C2 快速止损**：v20/v21 是 plugin 主线连续轮、v22 已定案、结论已进待办+Changelog，可先合并归档止血（不必等 A/B2 机制） |
| v11-② 规模膨胀类（规模触发） | 缓 | 分域（rules/ 目录）/ 活跃·历史分离（archive/）/ 标签+索引检索 / 生命周期状态机+写入冲突 Gate。**待活跃规则 >15 条或多域混杂**；当前单文件+索引表正是冷启动形态 |
| N3 / N4 / N6（flutter/） | 🟢 | 范围外，flutter 下一轮 |

> **Y 系列分组建议（v45 立 / v46 升级为三层结构，主控建议，采纳与否由评审定）**：
> **已决议（v47）：采纳三层结构 + 修正（Plan 模式拦一切写入含 docs/，落盘 = ExitPlanMode 获批后首步）——见 [优化方案-v47.md](优化方案-v47.md) §三决策 2；Y1-Y8 已全部落地关闭（v47 批次二，plugin 0.2.1），关闭记录见 [评审.md](评审.md) 已关闭表**
> - **Y1–Y4 同主题：评审全流程的用户可控性**——同根因：现行流程是「流转式」表述（1→2→3 顺流而下）而非「确认点」，AI 顺着流转走完，没有停顿机制。建议**一个方案整体设计**，修法采用**三层结构**：
>
> | 层 | 机制 | 角色 | 强度 |
> |---|---|---|---|
> | **外层（主修法）** | **Plan 模式物理边界**——评审-裁决-修订全程在 plan mode 内（harness 物理拦写），`ExitPlanMode` 由需求方亲自批准才放行实施 | 结构性保证「能不能开始写码」 | harness 级，绕不过 |
> | 内层 | AskUserQuestion 逐条裁决 | 粒度层「每条发现怎么处置」 | 中（模型自愿调用，非结构性——故只作粒度不作闸门） |
> | 底座 | 流程文档 Gate 化措辞 + 完成判据（落盘+可点击路径=步骤完成，Y2/Y4） | 定义流程与判据 | 弱（已被实战证伪单独使用） |
> | ~~hook~~ | ~~状态标记 + PreToolUse 拦写~~ | **不需要**——Plan 模式即其完美实现，免标记生命周期与「AI 自删标记」绕过洞 | — |
>
> - **关键洞察**：CLAUDE.md §3 软桥接早已映射「方案 Gate → EnterPlanMode/ExitPlanMode」，但评审闭环从未接到这堵墙上——**本次修法是接线，不是发明新机制**（复用平台原生实现）
> - **多轮天然支持**：裁决后改方案再评审，plan mode 内继续（docs/ 可写），ExitPlanMode 可多次进出，每轮批准都是一次真 Gate
> - **Y8 顺带受益**：plan mode 下写性 Bash（含 git commit）同样被 harness 拦——skill 内建 commit 步骤在评审阶段物理跑不动；实施开始后的提交纪律仍靠 §2 关键词（Y8 修法①措辞降级）——边界刚好分工
> - **Y5 独立**：评审维度扩充（范围是否扩大），落 plan-reviewer 与 review-review 两处清单
> - **Y6 顺手**：一行同步
> - 根因注记：与「纪律写了≠生效」同构；外层修法与根规则 §3 双 Gate 同构——**把「流转」接到真 Gate（plan mode）上，而非再造一个纸面 Gate**

> **v11 拆分依据**（决议留痕）：约束丢失（ConnectManager 回归 = compaction / 换会话 / AI 自信重推）**与规模无关**——1 条不变量也会丢；规模膨胀才需规模触发。故 INVARIANTS.md **不绑规模阈值**，分域/archive 绑。核心判断：**约束丢失 ≠ 规模问题**，按"问题类型"gate，不按"整体规模"。
> **INVARIANTS.md 比 business-rules 多的必须项**：每条带「可执行检查」字段（不变量天然可测，文档+测试双保险才有硬拦，否则又回软约束防不住破坏的老路）。
>
> 已办项不在此留档——关闭记录见 [`评审.md`](评审.md)「已关闭 / 已决议」表与 Changelog（如 D10：v33 轻装模式绕开 cache 快照性，v44 迁出本池）。
