# crules · 待办需求池

> **单一权威**：待办项（历轮评审遗留 + **需求方直接提出的优化需求**）的唯一记录处。
> v44 从 [`评审.md`](评审.md) 拆出——受众分离：本文件面向「要做什么」（需求方视角的行动池），评审.md 专注「发生了什么」（审计链）。
>
> **同步纪律（单向，防派生漂移）**：待办状态变化先在本文件更新，再到 [`评审.md`](评审.md) Changelog 记一行（评审只记轮次，**不重复待办正文**——避免双写不一致）。
> 符号：🔴 重要 / 🟡 中等 / 🟢 小优化；✅ 已落地（移 [评审.md](评审.md) 已关闭表）；🔒 已决议锁定；**缓** = 暂缓（等触发条件）

---

## 当前待办

| 项 | 级别 | 说明 |
|---|---|---|
| **V2 规则行为探针集** | 🟡 | 约 10 个 `claude -p` 探针断言核心红线（未说 commit 不提交 / 粘贴报错不改码 / 「直接改」不出范围 / 问不存在文件承认查无）；验收项含 Plan 模式接线四行为：派审走 Plan 模式、ExitPlanMode 后才落盘、写性 Bash 在 Plan 模式内被拦、docs/ 写入被拦（v47 批次二静态交付的运行时补验）。**v50 注：探针脚本资产已沉淀 `scripts/probes.sh`（10 探针，副作用级断言），本机 `claude -p` 通道阻断未执行（模型 glm-5.1 unrecognized → -p 挂死，逃生口变量亦不通）——环境恢复后 `bash scripts/probes.sh` 跑全套** |

> **Y1–Y8 已决议并全部落地关闭（v47 批次二，plugin 0.2.1）**：采纳三层结构（外层 **Plan 模式物理边界** / 内层选项卡只作粒度 / 底座确认点措辞；hook 方案明确不上）+ 修正 v46「plan mode 内 docs/ 可写」事实错误（Plan 模式拦一切写入，落盘 = ExitPlanMode 获批后首步）。设计全文与决策留痕见 [优化方案-v47.md](优化方案-v47.md) §三决策 2 / §5.1；关闭记录见 [评审.md](评审.md) 已关闭表；运行时四行为补验挂 **V2** 探针（本组块原文 v53 续精简，全文在优化方案内不丢失）

> **v11 拆分依据**（决议留痕）：约束丢失（ConnectManager 回归 = compaction / 换会话 / AI 自信重推）**与规模无关**——1 条不变量也会丢；规模膨胀才需规模触发。故 INVARIANTS.md **不绑规模阈值**，分域/archive 绑。核心判断：**约束丢失 ≠ 规模问题**，按"问题类型"gate，不按"整体规模"。
> **INVARIANTS.md 比 business-rules 多的必须项**：每条带「可执行检查」字段（不变量天然可测，文档+测试双保险才有硬拦，否则又回软约束防不住破坏的老路）。
>
> 已办项不在此留档——关闭记录见 [`评审.md`](评审.md)「已关闭 / 已决议」表与 Changelog（如 D10：v33 轻装模式绕开 cache 快照性，v44 迁出本池）。
