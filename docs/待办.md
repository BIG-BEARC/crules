# crules · 待办需求池

> **单一权威**：待办项（历轮评审遗留 + **需求方直接提出的优化需求**）的唯一记录处。
> v44 从 [`评审.md`](评审.md) 拆出——受众分离：本文件面向「要做什么」（需求方视角的行动池），评审.md 专注「发生了什么」（审计链）。
>
> **同步纪律（单向，防派生漂移）**：待办状态变化先在本文件更新，再到 [`评审.md`](评审.md) Changelog 记一行（评审只记轮次，**不重复待办正文**——避免双写不一致）。
> 符号：🔴 重要 / 🟡 中等 / 🟢 小优化；✅ 已落地（移 [评审.md](评审.md) 已关闭表）；🔒 已决议锁定；**缓** = 暂缓（等触发条件）

---

## 当前待办

| 项 | 级别 | 说明 |
|---|---|---|
| **W1 政策声明收敛治本** | 🟢 | v61 只修了四处头注（改引 MAINTENANCE 单一权威）；治本 = 把「进/不进 git」类政策声明收敛为一张政策表（MAINTENANCE 内），其余文件只链接，C 查扩展关键词扫描（「不进 git」必须出现在政策表或带链接）+ **J 查孪生表等价断言**（五维 IA 维支点：触发即更新表 / 文件结构表等孪生内容 hash 或行数等价断言，漂移即 CI 红；MAINTENANCE「自包含 vs 只留指针」定位矛盾一并解）——属设计新检查，单独一轮不顺手做（08-21 外审 + 五维审评建议） |
| **W2 外审剩余批次** | 🟡 | 08-21 外审（v61 收口 🔴 两项后剩余）：①轻装版本戳 + 导入漂移检查（Downloads 静默断链防线 + 完整模式增量合并基础——五维分发维支点）；②install.sh 安装脚本化（机械 cp 部分从模型方差转脚本确定性）；③元数据面收缩（meta-review 里程碑化——**与五维雷达重评同场**/ CHANGELOG 由 commit message 生成初稿；评审/待办合并与否待裁决）；④平台假设声明（README 环境要求：macOS/Linux，python3/fcntl）+ plugin 更新信任节（供应链）；⑤小项（uninstall 路径 / .claude/memory 与原生记忆撞名消歧 / init 问提速档 / 误拦取舍前移 README hook 节 / **检查器自测 fixture**——已知坏样本应报错断言，五维可维护维支点 / 触发词移动端形状的附录映射字段）——逐批放行 |

> **Y1–Y8 已决议并全部落地关闭（v47 批次二，plugin 0.2.1）**：采纳三层结构（外层 **Plan 模式物理边界** / 内层选项卡只作粒度 / 底座确认点措辞；hook 方案明确不上）+ 修正 v46「plan mode 内 docs/ 可写」事实错误（Plan 模式拦一切写入，落盘 = ExitPlanMode 获批后首步）。设计全文与决策留痕见 [优化方案-v47.md](archive/优化方案-v47.md) §三决策 2 / §5.1；关闭记录见 [评审.md](评审.md) 已关闭表；运行时四行为补验挂 **V2** 探针（本组块原文 v53 续精简，全文在优化方案内不丢失）

> **v11 拆分依据**（决议留痕）：约束丢失（ConnectManager 回归 = compaction / 换会话 / AI 自信重推）**与规模无关**——1 条不变量也会丢；规模膨胀才需规模触发。故 INVARIANTS.md **不绑规模阈值**，分域/archive 绑。核心判断：**约束丢失 ≠ 规模问题**，按"问题类型"gate，不按"整体规模"。
> **INVARIANTS.md 比 business-rules 多的必须项**：每条带「可执行检查」字段（不变量天然可测，文档+测试双保险才有硬拦，否则又回软约束防不住破坏的老路）。
>
> 已办项不在此留档——关闭记录见 [`评审.md`](评审.md)「已关闭 / 已决议」表与 Changelog（如 D10：v33 轻装模式绕开 cache 快照性，v44 迁出本池）。
