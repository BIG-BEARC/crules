# crules v47 实战优化 · 实施计划

> **归档注记（2026-08-21 v53）**：v47 四批次已全部落地并经独立 meta-review 抽查证实（见 `../评审.md` v47/v49 行与 `meta-review/`）；checkbox 未回勾系实施会话未回写，落地状态以评审.md 为准。

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** 落地 [优化方案-v47.md](优化方案-v47.md) 四批次：deny-list 5 向量修复 → Y1-Y8 三层结构接线 → 12 条复盘教训提炼 → 元文档收尾。

**Architecture:** 红→绿 fixture 先行的 hook 加固；流程文档从「流转式」改「确认点」并接上 Plan 模式物理边界（落盘 = ExitPlanMode 获批后首步）；复盘教训按普适度分流落规则；单 umbrella 评审轮（v47）四批分条记状态。

**Tech Stack:** Python3（hook + fixture）/ Bash（一致性脚本）/ Markdown（规则文档）/ claude plugin（bump + update + cache 验证）。

**本计划对 skill 模板的适配（仓库规则压过 skill 默认）：**

- **无 git commit 步骤**——CLAUDE.md §2 禁自动提交，skill 的「frequent commits」替换为**批末 Checkpoint**（贴验证证据 → 等需求方放行下一批 / 等 `commit` 指令）
- 逐批放行（方案 §九）：Task 5 / 11 / 15 / 18 是四个硬检查点，**未获放行不得进下一批**
- 探测纪律（[test_deny_list.py:7-9](../hooks/test_deny_list.py) 头注）：所有对 hook 的脚本探测输入必须 `json.dumps` 构造，禁 shell 手拼

---

## 批次一：C1 — deny-list 5 向量修复（🔴）

### Task 1: fixture 先行（红）

**Files:**
- Modify: `hooks/test_deny_list.py`（BLOCK_CASES 末尾 + ALLOW_CASES 末尾）

- [ ] **Step 1.1: BLOCK_CASES 追加（v40 段之后）**

```python
    # --- v47 批次一（08-18 外审 5 向量 + 裁定 B dry-run×force 信号即拦 + 空操作数）---
    "find . -name '*.log' | xargs rm -rf",     # 向量1：xargs 注入，rm 无显式操作数（paths 空集空真放行）
    "git branch --delete --force feat",        # 向量2：-D 长选项等价拼法
    "git branch -d -f feat",                   # 向量3：-D 分离旗标等价拼法
    "git push -fv origin main",                # 向量4：push 短旗标捆绑（-fq 同族）
    "git checkout -qf main",                   # 向量5：force_switch 捆绑 -qf ≠ -f
    "git push -fn origin main",                # 裁定 B：dry-run×force 组合信号即拦
    "git push --dry-run --force origin main",  # 裁定 B 同上（长选项组合）
    "git push -n origin +main",                # 裁定 B：dry-run + refspec 强推
    "git push --delete -n origin old-branch",  # 裁定 B：dry-run + 删远端分支
    "rm -rf build 2>&1",                       # 重定向 token 兼查：非白名单路径 + 重定向 → 仍拦
```

- [ ] **Step 1.2: ALLOW_CASES 追加（v40 段之后）**

```python
    # --- v47 重定向 FP 修复（重定向 token 不作 path 参与白名单判定）---
    "rm -rf /tmp/x 2>/dev/null",               # 白名单路径 + 重定向 → 放（当前误拦，修复后放行）
```

- [ ] **Step 1.3: 跑测试确认红**

Run: `python3 hooks/test_deny_list.py`
Expected: **失败 7**（6 应拦未拦：5 向量 + `git push -fn`；1 应放未放：`rm -rf /tmp/x 2>/dev/null`）。注：裁定 B 其余三条（`--dry-run --force` / `-n +main` / `--delete -n`）当前已被现有规则拦截，属行为锁定 fixture，非红样本。既有 66 条样本结果不动。

### Task 2: 修 deny-list.py（绿）

**Files:**
- Modify: `hooks/deny-list.py`（头注 / `force_switch` / push·branch·stash 分支 / rm 段）

- [ ] **Step 2.1: `force_switch` 改 parse_flags（拆捆绑）**

```python
def force_switch(seg_after_sub):
    """checkout/switch 带 -f/--force（含捆绑短旗标）且非建分支/指定源 → 强切丢弃未提交改动"""
    short, longs = parse_flags(seg_after_sub.split())
    force = "f" in short or "force" in longs
    exempt = any(c in short for c in "bcs") or bool(longs & {"branch", "create", "source"})
    return force and not exempt
```

- [ ] **Step 2.2: push 分支改 parse_flags（`if sub == "push":` 整块替换）**

```python
        if sub == "push":
            short, longs = parse_flags(seg.split())
            if "f" in short or "force" in longs:
                blocked("破坏性命令（git push --force）：请人工确认后自行执行；确需强推建议人工用 --force-with-lease")
            if "delete" in longs or re.search(r"\s:[^\s]", seg):
                blocked("破坏性命令（git push 删除远端分支）：请人工确认后自行执行")
            if re.search(r"(^|\s)\+\S+", seg):
                blocked("破坏性命令（git push +refspec 强制覆盖远端）：请人工确认后自行执行")
```

> 语义核对：`--force-with-lease` 走 longs 精确成员判定不受影响（既有放行 fixture 保持）；`--force-with-lease --force` 两长选项并存 → force 命中拦（既有拦截 fixture 保持）；裁定 B 使 `-fn` 短串含 `f` 即拦。

- [ ] **Step 2.3: branch 分支改 parse_flags**

```python
        elif sub == "branch":
            short, longs = parse_flags(seg.split())
            if "D" in short or (("d" in short or "delete" in longs) and ("f" in short or "force" in longs)):
                blocked("破坏性命令（git branch -D 强删分支）：请人工确认后自行执行")
```

- [ ] **Step 2.4: rm 段改（剥重定向 + 空操作数拦）——`if r:` 块内替换**

```python
    r = RM_SIG.search(seg)
    if r:
        tokens = seg[r.start():].split()  # rm 及其后 token（跳过前缀/包裹）
        short, longs = parse_flags(tokens)
        has_r = "r" in short or "recursive" in longs
        has_f = "f" in short or "force" in longs
        if has_r and has_f:
            REDIR = re.compile(r"^\d*[<>]")  # 重定向 token（2>&1 / 2>/dev/null / <file）不作 path
            paths = [os.path.normpath(os.path.expanduser(strip_quotes(t)))
                     for t in tokens[1:] if not t.startswith("-") and not REDIR.match(t)]
            if not paths or not all(p == "/tmp" or p.startswith("/tmp/") or p.startswith("/var/folders/") for p in paths):
                blocked("破坏性命令（rm 递归+强制，非临时目录或无操作数）：请人工确认后自行执行")
```

- [ ] **Step 2.5: 头注补一行（`# 决策边界` 行之前）**

```python
# v47 收口（红→绿 fixture 先行）：push/branch/force_switch 旗标判定统一走 parse_flags（拆捆绑短旗标，-fv/-qf/-D 等价拼法一次收敛）；
#   push dry-run×force 组合信号即拦（裁定 B：clean -nd 是正当诊断、push -fn 不是，不对称有理由）；rm 空操作数拦（deny-by-default：
#   空操作数 rm -rf 为静默 no-op，典型场景即 xargs/stdin 注入）；重定向 token 剥离后再判白名单（修 /tmp/x 2>/dev/null 误拦）
```

- [ ] **Step 2.6: 跑测试确认全绿**

Run: `python3 hooks/test_deny_list.py`
Expected: `deny-list 测试: N 拦 + M 放, 失败 0`（N/M 以基线+新增为准，新增 10 拦 + 1 放）。

- [ ] **Step 2.7: 一致性脚本（含 H 查回归）**

Run: `bash scripts/check-consistency.sh`
Expected: A–H 各节无 FAIL 输出，`== 完成 ==`。

### Task 3: bump + update + cache 验证 + e2e

- [ ] **Step 3.1: 版本号 0.1.9→0.2.0 双 json 同改**

`.claude-plugin/plugin.json`: `"version": "0.2.0"`；`.claude-plugin/marketplace.json` plugins[0]: `"version": "0.2.0"`。

- [ ] **Step 3.2: update + cache 验证（v31 完整规则：bump 必 update）**

Run: `claude plugin update crules@crules-market --scope user`（命令形态以当前 CLI 为准）
Verify: `ls ~/.claude/plugins/cache/crules-market/crules/` 最大版本 = 0.2.0；`grep -c "REDIR" ~/.claude/plugins/cache/crules-market/crules/0.2.0/hooks/deny-list.py` 输出 `1`（新代码已落 cache）。

- [ ] **Step 3.3: 安装态 e2e（必须在新 cache 确认后）**

在 `/tmp` 临时 git 仓库用 `claude -p` 免权限模式让模型执行 `git push -fv origin main`，预期：PreToolUse 拦截、模型停下转述「请人工确认后自行执行」。同步抽一条无害命令（`git status`）确认不受扰。

### Task 4: 批次一 Checkpoint（硬门）

- [ ] **Step 4.1: 汇报并等放行**

贴出：fixture 红绿两轮输出、一致性脚本输出、cache 版本 grep、e2e 拦截记录。**等需求方放行批次二**；未获 `commit` 指令不提交。

---

## 批次二：A 线 — Y1-Y8 三层结构接线

### Task 5: CLAUDE.md 三处

**Files:**
- Modify: `CLAUDE.md`（§2 bullet 追加 / §3 大需求强化段替换 / §六 bullet 追加）

- [ ] **Step 5.1: §2 bullet 列表末尾（「默认提交成功后推送」之后）追加**

```markdown
- **skill 冲突处理**：任何 skill 流程内建的 `commit` / `push` 步骤**一律不执行**，降级为「提示需求方可提交」后继续——本节优先级链（需求方指令 > skill > 默认）的执行细则；skill 步骤与本项目惯例冲突时惯例优先，不确定就问（实战案例见 `进阶/插件工作流.md`「优先级与冲突」）
```

- [ ] **Step 5.2: §3 大需求强化段整段替换**

原文（定位锚「**大需求强化**：新页面 / 跨模块 / 多端 / 大重构，方案产出后、实施前，派独立第三方 agent」…）替换为：

```markdown
> **大需求强化**：新页面 / 跨模块 / 多端 / 大重构，方案产出后、实施前，走独立第三方方案评审——派 `plan-reviewer`（独立上下文、只读、结论不过滤）前**停下来显式问需求方「是否派独立评审？」（默认必须派审，仅需求方明确选择跳过才跳过，按 Gate 例外记录理由）**；评审-裁决-修订全程在 **Plan 模式**内（`EnterPlanMode` 进、`ExitPlanMode` 需求方批准才放行实施）；**流转判据 =「评审结论在手且无待消解项」**——需求方推进指令不构成闭环豁免（在途评审先收结果或显式说明获确认）。完整闭环见 `进阶/方案评审闭环.md`。
```

- [ ] **Step 5.3: §六 bullet 列表末尾追加**

```markdown
- **派出 ≠ 完成**：后台 agent 派出时，表述必须标「进行中，结果待通知」——给 agent 的任务清单 ≠ 评审 / 调研结论；推进依赖其结果的下一步前，**先取结果**
```

### Task 6: 进阶/方案评审闭环.md 三处

**Files:**
- Modify: `进阶/方案评审闭环.md`（闭环流转 / 循环守卫 / 落盘约定）

- [ ] **Step 6.1: 「闭环流转」代码块整块替换**

```text
0. 【确认点】方案产出后，主控停下来问需求方「是否派独立评审？」——默认必须派审，仅需求方明确选择跳过才跳过（按 Gate 例外记录理由，方案 Gate 仍须走完）
1. 方案产出者输出方案文档（方案 Gate 产物 / PRD / spec / 任务拆分）
2. 主控派发第三方评审（独立上下文 subagent，prompt 只喂方案成品）
3. plan-reviewer 输出评审内容（返回值）；Plan 模式期间评审原文存会话，暂不落盘
4. 方案产出者读评审结论，逐条回应：
     · 评审不合理 → 指出 + 给原因
     · 评审合理   → 有无更优方案？有 → 给方案 + 原因（只提议，不改方案文档）
5. 【确认点】需求方逐条裁决（选项卡）；结论呈现后主控显式停下等待，禁止径直进实施
6. 改完 → 下一轮评审（可 diff 级）→ 循环
7. 判据：评审结论在手且无待消解项 + 需求方明示通过 → ExitPlanMode 请求批准 → 获批后第一个动作落盘评审记录 → 进实施
```

代码块后补一行说明：

```markdown
> 全程在 Plan 模式内：`EnterPlanMode` 进、`ExitPlanMode` 需求方批准才放行。Plan 模式拦一切写入（含 docs/），故评审原文在会话内流转、获批后统一落盘——多轮中间版本只存会话，最终一次性落盘（含首轮）。
```

- [ ] **Step 6.2: 「循环守卫（防卡死）」段追加一条**

```markdown
- **消解 ≠ 闭环**：消解本身是修改动作，可引入回归（实战 13 条消解 2 条带新缺口）。每轮消解后必须复审（可 **diff 级**——只评本轮改动），直到评审结论为「无须消解」才进实施；把「没动码」当「没推进」是弱化重犯——闭环判据是**评审结论到手且无待消解项**，不是主控自评来得及改
```

- [ ] **Step 6.3: 「落盘约定」节整节替换**

```markdown
## 落盘约定

- **落盘时机**：`ExitPlanMode` 获需求方批准后的**第一个固定动作**（Plan 模式拦一切写入含 docs/，评审原文在会话内流转，获批后统一落盘；方案文档内写明「第一步：落盘评审记录」）
- **落盘产物**：独立文件 `docs/评审-plan-<feature>.md`，含**各轮评审全文**（含首轮）与裁决记录，迭代标 v1 / v2 / …（与 `docs/评审-vN.md` 同风格）；方案文档只留路径 / 摘要，不承载评审正文
- 评审文档头部声明评审纪律（见上「评审纪律」）
- **写盘执行者**：plan-reviewer 无 Write 工具，评审文档由主控**原样落盘**（只机械转写返回值，不修改 / 不过滤）——独立性靠纪律保证，非靠 plan-reviewer 自己写盘
- **落盘即报**：落盘收尾必须附**文档名 + 可点击完整路径**（markdown 链接），需求方点开直接审阅
```

### Task 7: 进阶/工程化流程.md 两处

- [ ] **Step 7.1: §4 段落与代码块替换**

```markdown
PRD（§2）+ 任务拆分（§3）产出后、进入实施前，**停下来问需求方「是否派独立评审？」（默认必须派审，仅需求方明确跳过才跳过并记录理由）**，确认后派独立第三方 agent `plan-reviewer` 做方案技术评审。评审闭环、角色四分、流转、循环守卫、纪律全部见 `方案评审闭环.md`——本篇只规定**时机**；评审-裁决-修订全程 Plan 模式：

```text
方案成品（PRD + 任务拆分）→【确认点】问需求方是否派审 → 第三方方案评审 → 需求方逐条裁决 → 消解复审（diff 级，直到无须消解）→ ExitPlanMode 获批 → 落盘评审记录 → 实施
```
```

- [ ] **Step 7.2: §7 交付汇报模板「改动范围」节之后插入一节**

```markdown
## 落盘文档
- <文档名>：[完整路径](相对路径)——任何落盘动作（评审记录 / meta-review / 方案文档）收尾必须附可点击路径
```

### Task 8: 进阶/插件工作流.md + agents/plan-reviewer.md + .claude/commands/review-review.md

- [ ] **Step 8.1: 插件工作流「优先级与冲突」第一条 bullet 替换为**

```markdown
- **提交**：本包 [`../CLAUDE.md`](../CLAUDE.md) §2 的人工提交**压过** superpowers 的自动 commit。优先级链：需求方指令（CLAUDE.md）> skill > 默认行为。**实战案例（v47 落）**：brainstorming skill 流程自带「保存设计文档并 commit」步骤被照做 → 未经指令自动提交 → 需求方 reset 撤销——优先级链**存在但未被执行**的首次实战记录；§2「skill 冲突处理」条款为此而设（skill 内建 commit/push 步骤一律不执行，降级为「提示需求方可提交」）
```

- [ ] **Step 8.2: plan-reviewer.md 检查清单行——「范围是否溢出」措辞操作化**

原文「是否有更优解、范围是否溢出、」改为「是否有更优解、**范围是否扩大**（是否夹带已确认需求之外的改动——对照任务范围逐项核），」

- [ ] **Step 8.3: review-review.md 步骤 1 与步骤 2 改（Y6 + Y5）**

步骤 1 替换为：

```markdown
1. 主控读 `docs/评审.md` 当前态（最新详情 + 已关闭 + Changelog）+ [`docs/待办.md`](待办.md) 当前态（v44 起待办池拆出，评审只记轮次）
```

步骤 2「prompt 只喂评审.md 当前内容」改为「prompt 只喂评审.md + 待办.md 当前内容」。

步骤 3 审视清单追加一条 bullet：

```markdown
   - **范围是否扩大**：本轮改动 / 评审是否夹带已确认需求之外的项（对照 CLAUDE.md「守住范围边界」）
```

### Task 9: 批次二验收 + bump + Checkpoint（硬门）

- [ ] **Step 9.1: 语义级静态验收（逐条跑，贴输出）**

```bash
grep -rn "docs/ 可写" CLAUDE.md 进阶/ agents/ memory/ commands/ hooks/          # 预期零命中
grep -rn "plan mode 内继续" 进阶/ docs/评审.md                                    # 预期零命中（v46 建议原文已由 v47 决议修正）
grep -n "skill 冲突处理" CLAUDE.md 进阶/插件工作流.md                              # 预期两处命中且口径一致
grep -n "范围是否扩大" agents/plan-reviewer.md .claude/commands/review-review.md  # 预期两处命中
grep -n "待办.md" .claude/commands/review-review.md                               # 预期步骤 1/2 命中
grep -n "确认点" CLAUDE.md 进阶/方案评审闭环.md 进阶/工程化流程.md                 # 预期三处命中
bash scripts/check-consistency.sh                                                 # A–H 全绿
```

- [ ] **Step 9.2: bump 0.2.0→0.2.1 双 json + update + cache grep**

Verify: cache 最大版本 0.2.1 且 `grep -c "skill 冲突处理" ~/.claude/plugins/cache/crules-market/crules/0.2.1/CLAUDE.md` 输出 `1`。（`.claude/commands/review-review.md` 为仓库本地命令不走 plugin——本地核对即可，Step 9.1 已覆盖。）

- [ ] **Step 9.3: 汇报并等放行**——含诚实声明：本批证据等级为静态证据；运行时补验 = 下一个真实大需求实战观察三行为（派审确认点 / 裁决等待 / 获批后先落盘），发现偏差记待办回补。

---

## 批次三：B 线 — 12 条复盘教训提炼

### Task 10: CLAUDE.md §三 +3 / §四 +1

- [ ] **Step 10.1: §三 实施纪律末尾（「补丁死结止损」之后）追加**

```markdown
- **事实声明锚定出处**：设计 / 方案文档中的事实声明（接口路径 / 字段名 / 方法签名 / 资源存在性）必须附 `file:line` 出处，且**当轮核对原值**——从别处看到常量名 ≠ 核对过常量值
- **新建前先查既有**：新建文件 / 字段 / 组件前，先检索工程既有等价物（调研是双向 diff：目标行为 + 既有资产，漏一边返工或多做）
- **语义以来源链为准**：标识符语义以**值的来源链**（追写入点）为准，不以命名推断；跨系统对字段核对两侧写入点
```

- [ ] **Step 10.2: §四 验证（「构建 / 测试通过 ≠ 规范合规」条之后）追加**

```markdown
- **静默失败验到现象层**：无构建期信号的失败（资源加载 / 运行时懒加载 / 平台行为）必须验证到**可观察现象层**；配置 / 资源类改动按其生效条件验证（如需完全重启才生效的，热重载验证不算数）
```

### Task 11: 工程化流程 +2 节（§7 之后追加，不重编号）

- [ ] **Step 11.1: 文件末尾追加**

```markdown
## 8. 合并纪律（跨分支协作）

- **合并前探对方基线**：先在对方分支跑静态分析 / 测试探基线——对方质量不可假设（不跑测试的提交、删码不删测试），合并 = 把对方技术债一次性接进自己分支，预留修复时间
- **格式化提交复活删除码**：合并含**全量格式化提交**的分支，冲突解决须逐文件对照双方意图——格式化让 git 失去行邻近性，「我方删除类」改动极易被带回

## 9. 复刻 / 对齐任务的对照清单

复刻 / 对齐类任务（UI 对齐、跨工程移植、规范迁移）：方案 Gate 产物必须含**逐行对照清单**——基准值 / 当前值 / 动作三列，逐项注明基准出处 `file:line`；实施按清单执行，清单外改动按范围外处理。这是「参照物优先」纪律的**产物形态**（实战 3 轮 UI 对齐验证有效）。
```

### Task 12: agents/error.md +1 条

- [ ] **Step 12.1: 「根因定位方法论」节末尾（「修一坑查同类」之后）追加**

```markdown
- **长输出先预处理再读**：测试 / 构建长输出的紧凑进度行（`\r`）会吞掉失败信号——预处理（`\r`→`\n`）后再读；定位时分目录 / 单文件复跑做二分
```

### Task 13: README 口径 + bump + Checkpoint（硬门）

- [ ] **Step 13.1: README「精髓」行口径同步**

「（与 `项目附录.md` 合计实测 ≈4–5k token）」改为「（与 `项目附录.md` 合计 ≈4.6–5.3k token；v47 提炼 +4 条实施 / 验证纪律后的口径）」。

- [ ] **Step 13.2: 落点齐全自检**

```bash
grep -n "事实声明锚定出处\|新建前先查既有\|语义以来源链为准" CLAUDE.md   # 三条命中（①③④）
grep -n "静默失败验到现象层" CLAUDE.md                                    # 命中（⑦）
grep -n "合并纪律\|对照清单" 进阶/工程化流程.md                           # 两节命中（⑧⑨⑫）
grep -n "长输出先预处理再读" agents/error.md                              # 命中（⑪）
bash scripts/check-consistency.sh                                         # 全绿
```

- [ ] **Step 13.3: bump 0.2.1→0.2.2 双 json + update + cache grep**（验 `grep -c "事实声明锚定出处" .../0.2.2/CLAUDE.md` = 1）

- [ ] **Step 13.4: 汇报并等放行**

---

## 批次四：C2 — 元文档收尾

### Task 14: CHANGELOG 修序

- [ ] **Step 14.1: 移动 v29 / v28 两个段落到 v30 与 v27 之间**（当前错插在 v40 与 v39 之间；本文件最新在前，08-14 内顺序应为 v33→v31→v30→v29→v28→v27）
- [ ] **Step 14.2: 自此各新段遵守章程口径**：只记「新增 / 删除文件 + 一句话能力」，叙事归评审.md（对既有超口径段落不回改，只约束新增）

### Task 15: 待办.md 更新

- [ ] **Step 15.1: Y1-Y8 八行移出当前待办表**（关闭记录由 Task 16 落评审.md 已关闭表）
- [ ] **Step 15.2: 「Y 系列分组建议」块标注决议**：开头加「**已决议（v47）：采纳三层结构 + 修正（Plan 模式拦一切写入含 docs/，落盘 = ExitPlanMode 获批后首步）——见 [优化方案-v47.md](优化方案-v47.md) §三决策 2**」
- [ ] **Step 15.3: 新增 4 行待办**

```markdown
| **V1 CI 挂一致性脚本与 fixture 回归** | 🔴 | GitHub Actions push 时跑 `scripts/check-consistency.sh` + `python3 hooks/test_deny_list.py`——治「记得跑」，L14 治理最后一公里（v47 批次四登记；**已于 v48 落地关闭**，见 docs/评审.md 已关闭表） |
| **V2 规则行为探针集** | 🟡 | 约 10 个 `claude -p` 探针断言核心红线（未说 commit 不提交 / 粘贴报错不改码 / 「直接改」不出范围 / 问不存在文件承认查无）；验收项含 Plan 模式接线四行为：派审走 Plan 模式、ExitPlanMode 后才落盘、写性 Bash 在 Plan 模式内被拦、docs/ 写入被拦（v47 批次二静态交付的运行时补验） |
| **V3 Gate 信任分级设计** | 🟡 缓 | 连续 N 任务零返工 → 需求方显式调档扩大极简范围（治 gate 疲劳橡皮图章）；属新机制，单独 brainstorm |
| **V4 接口演进同步测试 fake（教训⑩）** | 🟢 | 项目侧约定（v47 裁定不入本包通用规则），消费工程吸收：改接口 / 签名必须同步其测试与 mock/fake 并跑全量——实战第二次出现（状态图复盘合并教训 3） |
```

### Task 16: 评审.md v47 umbrella 轮登记

- [ ] **Step 16.1: Changelog 表加一行**

```markdown
| v47 | 08-20 | **实战优化四批次**（方案 [优化方案-v47.md](优化方案-v47.md)，经独立 plan-reviewer 12 条消解 + 裁定 B）：①deny-list 5 向量修复（parse_flags 统一 / 裁定 B 信号即拦 / 空操作数拦 / 重定向 FP 修，fixture +10+1，plugin 0.2.0）②Y1-Y8 三层结构接线（Plan 模式外层 + 确认点 + 落盘 = 获批后首步，plugin 0.2.1）③复盘教训 ①③④⑦⑧⑨⑪⑫ 提炼落规则（⑩ 裁定入待办 V4，plugin 0.2.2）④元文档收尾（CHANGELOG 修序 + 新待办 V1-V4）。Y1-Y8 全部落地关闭 |
```

- [ ] **Step 16.2: 最新详情段**：v4x 前段按 L11 移 archive（如已是单段则直接覆写），写 v47 详情单段（四批次落地证据摘要）
- [ ] **Step 16.3: 已关闭表加 Y1-Y8 合并行**（Y1+Y3 / Y2 / Y4（含 Y7）/ Y5 / Y6 / Y8 各一句处置 + 验证等级标注：批次二为静态证据）

### Task 17: 终检 + 终 Checkpoint

- [ ] **Step 17.1: L14 总览同步检查**：本轮零模板文件增删（docs/ 新增两文件属不复制资产），README 清单无涉——核对并在汇报中留痕
- [ ] **Step 17.2: 全量一致性**：`bash scripts/check-consistency.sh` A–H 全绿
- [ ] **Step 17.3: 汇报终检**（四批次证据汇总 + V1-V4 新待办清单 + 全程未 commit 声明）——**等 `commit` / `push` 指令**

---

## 计划自审记录

- **Spec 覆盖**：方案 §四→Task 1-4；§五→Task 5-9；§六→Task 10-13（⑩→Task 15.3 V4，裁定一致）；§七→Task 14-17；§4.2-5 重定向 FP→Task 1.2/2.4；§5.2 语义验收→Task 9.1；§5.2 天然验收场→Task 9.3 汇报词。无缺口。
- **占位符扫描**：无 TBD / 「适当处理」类；全部步骤含完整代码或精确替换文本。
- **一致性核对**：`parse_flags` 返回 `(short, longs)` 与 Task 2 各处用法一致；版本链 0.2.0→0.2.1→0.2.2 与 cache grep 样本逐批对应；`REDIR` 定义与 Step 3.2 grep 目标一致。
