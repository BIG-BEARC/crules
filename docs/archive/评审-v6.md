# 通用项目规则模板包 · 评审文档（v6）

> **本版性质**：v5 落地核对 + 剩余可改项。定位不变——spec-kit + superpowers 的通用约定层 + 技术栈目录，核心原则渐进增强。
> **符号**：🔴 重要 / 🟡 中等 / 🟢 小优化；✅ 已落地 / ⚠️ 部分落地 / ❌ 未落地。
> 本文档仅为评审，未改动任何规则或代码文件。

---

## §1 本轮落地总评

v5 的**最难架构项**都解决得干净：增强层混合方案、main 分档、Flutter 分层挪移、MAINTENANCE 去重，并额外补了 `进阶/README.md`（启用决策表）这个 v5 未要求的缺口。统计：✅ 16 项、⚠️ 7 项、❌ 9 项。剩余主要是 README 入口、A' 红线同步两个"搬运/同步"活，加几个泛化/桥接小尾巴——**无新的设计性硬伤**。

---

## §2 v5 落地核对

### ✅ 已落地（16 项，质量高）

| v5 条目 | 落地情况 |
|---|---|
| §1.1 增强层混合方案 | CLAUDE.md §3"软桥接"段 + `插件工作流.md` 三列映射（内置兜底/superpowers/spec-kit）；明确"硬约束靠 Plan 模式兜底，skill 为增强"——**超出建议** |
| §1.3 分层挪移 | Flutter 角色→`flutter/agents/`；main.md 移除（main=主对话）；Flutter checklist→`flutter/checklist.md` |
| 2.6#2 main 分档 | `Agent编排.md`"主控职责分档"表 + "不是绝对禁令，是按规模分档"——**完美** |
| 2.6#3 main 形态 | 选 (a)："主控即主对话，不是独立子代理"，落地 |
| 2.7#2 `/update-memory` | `commands/update-memory.md` 命令文件 + 记忆库体系指向它 |
| 2.9#2 MAINTENANCE 去重 | 80 行重复 → 40 行运行时卡，指向进阶篇 |
| 2.9#1 / 2.9#3 | NAVIGATION 泛化（"非 Flutter 按栈替换"）+ indexes 标注"启用时创建" |
| §5.1 | `进阶/插件工作流.md` 新增 |
| 2.1#1 / #2 / #4 / #7 | §3 软桥接、顶部定位、附录"按层选用"、版本号 v1.1 |
| 2.3#2 / 2.8#1#2#3 / 2.9 全 | 附录按层、main 机制、Flutter 角色挪移、agents 路径说明 |
| **额外补的** | `进阶/README.md`（启用决策表+路径约定）、`flutter/checklist.md`（专项 checklist+模块清单+反模式） |

### ⚠️ 部分落地（7 项）

| v5 条目 | 已做 | 还差 |
|---|---|---|
| 2.4#1 checklist 泛化 | 加"框架专项见 flutter/checklist"出口 | 通用条目本身（视图→状态→数据→网络、蓝牙）仍移动端底色 |
| 2.5#1 模块清单泛化 | 加"通用基线，Flutter 专项见 flutter/checklist" | 模块（UI/视觉/平台/原生）仍 Flutter 底色 |
| 2.4#2 / 2.5#3 / 2.6#1 桥接 | `插件工作流.md` 已映射 superpowers | 审查纪律/工程化/Agent编排 各篇**未引用**插件工作流.md |
| 2.1#3 §7 入口 | 加了"插件工作流"行 | 仍缺 `agents/`、`memory/` 入口 |
| §5.2 ownership 表 | `flutter/README:220` 有一句指引 | 非正式表（缺"哪些同步/哪些独有"结构） |

### ❌ 未落地（9 项）

| v5 条目 | 说明 |
|---|---|
| **2.2 README.md** | 完全未改（10:55 旧版），仍"任何语言/框架通用"+"Flutter 用 ../flutter/"，与整体定位脱节 |
| **§1.2 A' 同步** | flutter/app、plugin（8/7 未改）无 `AUTO-SYNC`、无脚本、红线仍落后通用包 |
| 2.5#2 PRD↔spec-kit | 工程化流程 §2 仍"主控产出 prd-<feature>.md"，没接 `/speckit` |
| 2.1#5 方案 Gate 裁剪 | §3 方案 Gate 仍七项，未注"按规模裁剪" |
| 2.1#6 / 2.7#3 记忆库加载文案 | 记忆库体系说"CLAUDE.md 应声明"，但 CLAUDE.md 无此段、无现成文案 |
| 2.7#1 spec-kit 边界 | 记忆库体系未划与 spec-kit constitution/specs 的边界 |
| 2.4#3 案例脱敏 | "2.10.6 称重"代号仍在（审查纪律:111） |

---

## §3 剩余可改项（对照修改）

### 🔴 P1（最该先处理）

**1. README.md 跟上定位**（v5 2.2 全部）
- 位置：`README.md` 第 3、5 行 + 全文
- 问题：仍是旧版"适用于任何语言/框架"+"Flutter 项目用 `../flutter/` 更贴"，与"spec-kit+superpowers 通用约定层 + 技术栈目录 + 渐进增强"的整体定位脱节。README 是入口，误导性最强。
- 建议：重写定位段为"通用约定层 + 技术栈目录"；补工具链说明（spec-kit/superpowers 可选，启用映射见 `进阶/插件工作流.md`）；补复制目标路径（`agents/`→`.claude/agents/`、`memory/`→`.claude/memory/`、`commands/`→`.claude/commands/`）；来源说明的内部代号脱敏。

**2. A' flutter 红线同步**（v5 §1.2）
- 位置：`flutter/app/CLAUDE.md`、`flutter/plugin/CLAUDE.md`
- 问题：两文件 8/7 未改，红线已是通用包几轮前的旧版（缺不虚构、范围边界、敏感数据兜底、提交授权边界）；无 `AUTO-SYNC` 标注、无同步脚本；ownership 仅 `flutter/README:220` 一句。
- 建议：flutter/app、plugin 的重复段（红线/提交/验证/后台diff）加 `<!-- AUTO-SYNC FROM 通用项目规则模板包/CLAUDE.md §X -->`；建同步脚本（按锚点抽取替换）；把 ownership 正式化为表（见 P2#7）。最低限度：先手动同步一次红线，止住漂移。

### 🟡 P2

**3. "启用判断"机制显式化**（新发现，替代 v5 2.3#1）
- 位置：`CLAUDE.md` §3 软桥接段、`插件工作流.md`
- 问题：§3 说"启用 superpowers/spec-kit 时叠加 skill"，但没说 AI **如何判断**是否启用；附录也未加工具链配置位。
- 建议：二选一——(a) 加一句判断方式："会话开始检查 skill/命令可见性（`brainstorming`、`/speckit` 等），含则视为启用"；(b) 仍用附录工具链配置位（v5 2.3#1 原方案）让用户显式声明。

**4. PRD ↔ `/speckit` 接通**（v5 2.5#2）
- 位置：`工程化流程.md` §2 PRD
- 问题：仍"主控产出 `prd-<feature>.md`"，与 spec-kit 的 spec 重复造轮子。
- 建议：声明"启用 spec-kit 时用 `/speckit` 产出 spec/constitution，本篇只规定其节奏（分模块逐段确认）；未启用则人工 prd"。

**5. 各进阶篇引用 `插件工作流.md`**（v5 2.4#2 / 2.5#3 / 2.6#1）
- 位置：`审查与复核纪律.md`、`工程化流程.md`、`Agent编排.md`
- 问题：`插件工作流.md` 是新的 skill 映射权威，但这三篇没指向它（只有 CLAUDE.md §3/§7 指了）。
- 建议：各篇相关段加一句"启用插件时的 skill 映射见 `插件工作流.md`"。

**6. §7 加 agents/memory 入口**（v5 2.1#3）
- 位置：`CLAUDE.md` §7 扩展入口表
- 问题：表里缺 `agents/`、`memory/` 两个配套目录。
- 建议：加两行——"多 agent 角色 / 记忆库维护"。

**7. ownership 正式表**（v5 §5.2）
- 位置：建议放 `flutter/README.md` 一节或独立文件
- 问题：仅 `flutter/README:220` 一句话，非结构化表。
- 建议：正式化——
  | 段 | 权威 | flutter/app 处理 |
  |---|---|---|
  | 红线/提交/证据 | 通用包 | `AUTO-SYNC` 同步 |
  | superpowers 工作流映射 | 通用包 `插件工作流.md` | 同步，不自写 |
  | dart-flutter 映射/技术栈/分层/TDD/checklist/agents | flutter/ 独有 | 自维护 |

### 🟢 P3（小优化）

| # | 位置 | 建议 |
|---|---|---|
| 8 | 审查纪律:111 | "2.10.6 称重"内部代号脱敏 |
| 9 | CLAUDE.md §3:117 | 方案 Gate 七项加"按规模裁剪，极简只需文件+修改方式+验证" |
| 10 | CLAUDE.md §7 后 | 补记忆库加载声明可粘贴文案（NAVIGATION/MAINTENANCE/patterns） |
| 11 | 记忆库体系.md | 划与 spec-kit constitution/specs 的边界（spec-kit 管 spec，记忆库管索引/决策/模式） |
| 12 | 审查纪律/工程化流程 | 通用 checklist/模块清单条目去移动端假设（视图→状态→数据→网络、蓝牙等），或标注"以移动端为例，按栈替换" |
| 13 | 插件工作流.md:20 | `/specify` `/tasks` 命令名核实——未确认是 spec-kit 真实命令；建议改为"spec-kit 的任务产出命令"等不绑死版本的说法（作者已在 :41 自标注免责，仍建议软化） |

---

## §4 新发现

**`插件工作流.md:20` 命令名准确性**：实施阶段写 spec-kit 用 `/specify` `/tasks` 产出任务。前几轮核实 spec-kit 时只确认了 `/speckit` 主命令，`/specify`、`/tasks` 未能证实。作者已在 :41 自标注"spec-kit 命令集仍在演进，落地前请核实"——免责到位；仍建议把具体子命令名软化（见 P3#13），避免命令名过期误导。

---

## §5 推荐修改顺序

| 阶段 | 内容 |
|---|---|
| **P1** | README 跟上定位（§3#1）；A' flutter 红线同步（§3#2）——至少先手动同步止漂 |
| **P2** | 启用判断显式化（#3）、PRD↔speckit（#4）、各篇引用插件工作流（#5）、§7 入口（#6）、ownership 表（#7） |
| **P3** | #8–#13 小优化 |

---

> 本文档随评审迭代（当前 v6）。P1/P2 处理后可出 v7 收尾。
