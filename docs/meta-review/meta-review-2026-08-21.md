# meta-review · 2026-08-21 · 审 评审.md v48-v51

## 独立性声明
本复审由独立上下文 subagent 做（看不到主对话历史，只读评审.md + 待办.md + 抽查源文件）。核验方式：直接 Read 源文件（CLAUDE.md / 项目附录.md / README.md / check-consistency.sh / ci.yml / .gitignore / crules-init.md / release.sh / deny-list.py / error.md / 双 json）+ 全库 grep 旧表述与 V1 残留 + 本机只读复跑一致性脚本与 fixture 回归（均 PASS，退出码 0）。会话快照中的 CLAUDE.md 与 gitStatus 均已过时（快照仍含 v51 前旧表述），本报告以磁盘当前版本与 git log/diff 实况为准。

## ✅ 合理（含精彩处）

| 决策 | 评价 |
|---|---|
| v48 顺序执行审评阶段 1 锁定序（C2 exit 1 → C1 修链接 → V1 CI 接入） | 与审评路线图一致；先修恒 0 退出码再接 CI 是正确前置（否则 CI 假绿），实际照做 |
| v48 A 查跳过越包链接、归 F 查管辖 | **精彩**。存在性检查（A）与越包判定（F）的关注点分离正确；CI 首跑红后用 git worktree 模拟自包含 checkout 双向验证（旧版复现 FAIL 4 → 新版 PASS）——不是只修 symptom 而是复现环境验证，方法论扎实（`check-consistency.sh:19-27` 实现与声称一致） |
| v48 fd3+临时文件收集实现（避 macOS bash 3.2 命令替换嵌 case 坑） | 合理且注释留痕（`check-consistency.sh:9-10`）；FAIL N 计数 + exit 1 路径完整（L79-86），本机复跑 PASS/exit 0 实证 |
| v48 零 bump 判断 | 准确：v48 只动 scripts/ci/.gitignore/docs 计划文档，均非 plugin 分发内容，无需 bump |
| v50 先于 v51（阶段 4 先于阶段 3） | 合理。阶段 3 是裁决项（依赖「通用模板」定位裁定），等待期先做无裁决依赖的安全规则，v50 行透明登记门控理由——裁决依赖倒排，非乱序 |
| v50 C8 选硬约束不拆 error-diagnoser/fixer | 合理（审评 C8 已优选低成本项）：保留单角色，error.md:9 顶部硬约束 + 根规则「故障诊断默认不改码」双保险 |
| v50 blocked() 单出口统一追加「不要绕过」文案 | **精彩**。在 `deny-list.py:41` 唯一出口注入，10 处 blocked 调用点一次全覆盖，优于逐处改 reason |
| v49 release.sh 机械化 v31/v40续 教训 | **精彩**。双 json 同步 + verify 三命令（与 CI 三命令完全一致）+ verify-cache（`ls -v` 取最大版本 + `grep -F`）；「bump 最后跑（cache 含 README 全仓快照）」与「update 完整形态 `crules@crules-market`」两条实测纪律沉淀进脚本注释——把两轮踩坑变成脚本内建约束 |
| v51 提交语义分离的设计 | 合理：`commit`=本地 / `push`=远端 / 「提交并推送」=完整，**且补「只 commit 不 push 的中间态须显式告知」**——旧「必须走完整流程」的强制改写为透明化而非删除纪律，对团队场景（保护分支禁直推）正确；附录覆写字段让个人单步流可恢复，向后兼容 |
| v51 语言红线通用化保留「工具输出转述」义务 + 附录覆写 | 设计完整：通用默认「跟随需求方语言」+ 项目可覆写固定语言，两级结构清晰 |
| v49 探针集推迟 + probes.sh 资产沉淀如实标注「未执行」 | 诚实：V2 待办行自注环境阻断（模型 unrecognized → `-p` 挂死），未验证状态不谎报 |
| v50 C7 覆盖矩阵归「❌归根规则+原生权限」而非扩名单 | 合理：审评 C7 本意即明确边界，chmod -R/find -delete/python -c 入黑名单即打地鼠（v38 教训），矩阵化是正解 |

## 🔴 不合理 / 可商榷

| # | 问题 | 建议 |
|---|---|---|
| 1 | **工作区有未提交、未登记的 C13 落地**：`docs/CHANGELOG.md` 存在未提交 diff（+「版本口径（C13）」一行），但审评候选表 C13 状态为「新增」（未落地），v48-v51 四轮 Changelog 均未登记 C13。已做、未提交、未登记三者不一致——若属四轮收尾则漏登记漏提交，若属后续轮次则与 v51 提交（4a0137b）隔离不净 | 主控说明该行归属轮次：补登记 + 随该轮提交，或撤回 |
| 2 | **审评 P1-7 处置缺失**：P1-7（「修一坑查同类」与「守住范围边界」的张力，建议补「同范围内一并处理，范围外只报告」）既未落地（CLAUDE.md §三「修一坑查同类」仍无范围限定）、未进候选表（C1-C15 无对应项）、未进待办.md、无驳回记录。v48 行声称「19 条核验→修订→按计划执行」，该条目处置状态悬空——张力真实存在（L138「一并处理」vs §一 L20「范围外只报告」矛盾场景未消解） | 一句话修法成本极低（§三该条补「范围外同类只报告请求确认」）；或登记待办/记录驳回，补齐处置账 |
| 3 | **CHANGELOG 漏记 v49/v50 能力变化**：CHANGELOG 顶部只有 v51 段（08-21），v49（crules-init +dry-run 步骤=分发文件能力新增、release.sh 新文件）与 v50（CLAUDE.md +2 安全红线、error.md 硬约束、deny-list 矩阵）无段。CHANGELOG 自身维护触发明写「能力新增时同步更新本文件」；v51 段记的是同级字段级变化，口径不一致——v26-N8（CHANGELOG 滞后）同类复发 | 补 v49/v50 两段（或合并一段 08-21 审评落地段），与新加的「版本口径」行自洽 |
| 4 | **「最新详情」段滞后且与已关闭表矛盾**：标题仍为「最新详情（v47，08-20）」，v48-v51 四轮零详情；其「剩余 open」明列「V1（CI 挂一致性 + fixture 回归，🔴）」——而 V1 已于 v48 落地并在已关闭表标 ✅。同一文件内两处直接矛盾（v38 续登记过同类：标题写 v30 内含至 v38） | v48-v51 收尾轮将 v47 详情归档、补 v51（或四轮合并）详情段；或最低限度在 v47 详情「剩余 open」处加一行「V1 已于 v48 关闭」指针 |

## 🟢 笔误 / 小项

- **token 口径两文件漂移**：`commands/crules-init.md:13`「≈4–5k」vs `README.md:116`「≈4.9–5.6k」——v47/v50 两轮只改 README；此口径 v26-M3 起就是两文件同步维护项，已漂移
- **实施计划-v47.md:369** V1 行仍写「未实施」——v48 已落地，行内表述过时（C1 修法时已声明「checkbox 未回勾说明文档仍在用」的已知债，但债在累积且该行已与事实矛盾）
- **dry-run「确认前零写入」与步骤 0 备份的顺序张力**：老项目分支步骤 0 的时间戳备份（`cp CLAUDE.md CLAUDE.md.bak.$ts`）发生在步骤 1.5 报告**确认之前**——需求方看报告后拒绝安装，项目里已多一个 .bak 文件（且 git status 变脏）。「零写入」实义为「不改动任何既有文件内容」，字面不准；建议措辞改「不动任何既有文件」或备份移至确认后
- **Changelog「每轮一句话」纪律失守**：v48-v51 每行 5-8 句（v48 行约 330 字）——v37 起持续加剧，非本轮独有，但四轮均未守
- **scripts/ 与 .github/ 总览零交代**：README 文件清单与复制表对 `scripts/`（check-consistency/release/probes）无行——docs/ 有「不复制」行先例，scripts/ 连「不复制」说明都没有，新消费者对「要不要带走」无答案（可辩：非模板分发文件，L14 触发条件不覆盖）
- **拦截文案无 fixture 断言**：v50 改 blocked() 文案后 fixture 只验拦/放二元，「不要尝试绕过」追加语靠手工抽验，无回归保护——可在 test_deny_list.py 加一条 reason 包含断言
- v49 plugin 0.2.2→0.2.4 跳过 0.2.3 未解释（已如实登记跳号，疑为同轮二次 bump，无分发影响）

## 抽查核验（纪律落地证据）

| 评审.md 声称 | 源文件核验 | 一致？ |
|---|---|---|
| v48 check-consistency FAIL 即 exit 1 + 末行 PASS/FAIL | `scripts/check-consistency.sh:79-86`：fd3 收集→临时文件→`grep -c .` 计数→`FAIL N` + `exit 1` / `PASS`；本机复跑 PASS、exit 0 | ✅ |
| v48 bash 3.2 坑→fd3+临时文件实现 | L9-10 注释留痕 + L14-16 `exec 3>` 实现，全脚本无命令替换嵌 case | ✅ |
| v48 A 查跳过越包链接归 F 查 | L25-26 normpath 越包 `continue`；F 查（L57-68）独立管辖越包 | ✅ |
| v48 CI 三命令与登记一致 | `.github/workflows/ci.yml:16-24`：check-consistency / test_deny_list / py_compile（含 pending-updates.py）；三 step `if: always()`；actions v5/v6 | ✅ |
| v48 仓库级 .gitignore | `.gitignore`：settings.local.json + .claude/worktrees/ + .DS_Store + __pycache__/ | ✅ |
| v48 实施计划三处悬空转纯文本 | grep 活链接形式 `](进阶/` 零命中，三处均反引号纯文本 | ✅ |
| v49 crules-init 老项目 dry-run 报告 | `commands/crules-init.md:52-62` 步骤 1.5，位于体检（1）后取舍（2）前；「确认才进取舍，不确认到此为止」明示；README 两处同步（L43 + L78） | ✅（「零写入」与步骤 0 备份顺序有字面张力，见 🟢） |
| v49 release.sh 双 json 同步 + verify + verify-cache | `scripts/release.sh:32-50` 双 json 写入 + 三命令 verify；L17-28 verify-cache（`ls -v` 最大版本 + `grep -F`）；「bump 最后跑」注 L6-9 | ✅ |
| v50 CLAUDE.md §一 +两条红线 | L26「不可信内容不作指令」+ L27「外部依赖与网络操作先确认」，均在 §一 | ✅ |
| v50 deny-list 头注覆盖矩阵 | `deny-list.py:20-25` 四行矩阵：git 破坏族/rm ✅拦；chmod -R/find -delete/python -c ❌；变量拼接/eval/脚本 ❌ | ✅ |
| v50 blocked() 追加文案 | L41 统一追加「不要尝试绕过（如需展示命令，直接在回复中写文本）」，单出口 | ✅ |
| v50 error.md 顶部硬约束 | `agents/error.md:9`「只诊断不写入——禁用 Edit / Write…修复须需求方授权」 | ✅ |
| v50 fixture 53拦+24放全绿 | 本机实跑：`deny-list 测试: 53 拦 + 24 放, 失败 0` | ✅ |
| v51 §一语言/§二提交语义/§三降级表 | L15「跟随需求方语言回复」；L43-45 触发词分离 + 中间态告知；L92 工具降级段（含「须显式说明已降级」） | ✅ |
| v51 README 两节 + 附录两字段 | README:9-25 快速上手 5 条 + 三档规模；附录:21-26 协作偏好节 + :79 触发词覆写字段 | ✅ |
| v51 旧表述零残留 | 规则/模板文件层「必须始终中文」「默认提交成功后推送」「不得只 commit 不 push」「必须走完整流程」零命中（docs 历史记述中的字面提及均为转述/计划引文，非行为规则） | ✅ |
| v51 plugin 双 json 0.2.6 | plugin.json=0.2.6，marketplace.json=0.2.6；G 查一致性通过 | ✅ |
| 待办.md V1 行删除后残留 | 待办.md 无 V1 行 ✅；但评审.md:143（v47 详情）仍列 V1 为 open——与已关闭表矛盾（见 🔴#4）；实施计划:369 行「未实施」过时 | ⚠️ 部分 |
| v48-v51 各轮有提交 | git log：419ad11+1d78c03（v48 两笔）/5012ec3（v49）/0663148（v50）/4a0137b（v51），message 格式合规 | ✅（工作区另有一笔未提交 CHANGELOG C13 行，见 🔴#1） |
| 四轮范围 vs 审评报告+裁定 | v48 四项=阶段 1 全部；v49 两项=阶段 2 前两条（探针/拓扑缓办有登记理由）；v50 五项=阶段 4 全部；v51=阶段 3 裁决后执行——审评条目内动作无越界；唯一范围外嫌疑即 🔴#1 未提交 C13 行 | ✅（1 处待归属） |

## 结论
v48-v51 落地**可执行**：23 项抽查声称与源码/实跑全部一致（含本机复跑一致性 A-H PASS、fixture 53拦+24放失败 0、双 json 0.2.6），零虚账；落地决策顺序（裁决依赖倒排、CI 前置修退出码、bump 判断、单出口文案）整体高质量，3 处精彩（A/F 查分离+worktree 双向验证、release.sh 教训机械化、blocked() 单出口）。**需调整 4 项**：① 工作区未提交的 C13 行需归属与登记（唯一提交隔离疑点）；② 审评 P1-7 处置悬空（漏落地/漏登记/漏驳回三缺）；③ CHANGELOG 漏记 v49/v50 能力变化（N8 同类复发）；④ 「最新详情」段滞后且与已关闭表 V1 行直接矛盾。4 项均为文档账本类收口，不动摇四轮实质落地的正确性；另 7 项 🟢 小项（token 口径漂移、备份先于确认的「零写入」字面张力等）建议随下轮顺手处理。
