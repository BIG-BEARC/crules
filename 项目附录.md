# 项目附录

> **【复制后必填】** 本文件填的是**项目特有信息**。`CLAUDE.md` 是通用规则（任何项目都一样），本文件是每个项目不同的那部分。
>
> 【必填】只 3 项（项目名 / 技术栈 / 构建·分析·测试命令），填完即让 `CLAUDE.md` 完整生效；其余【可选】，不填不影响通用规则（通用兜底见 `CLAUDE.md` §一 / §四）。
>
> 通用规则不在此处重复——需要时翻 `CLAUDE.md`。

---

## 项目名称【必填】

- 项目名称：

## 仓库与授权【可选】

- 允许修改的仓库：
- 只读参考源（可读取但不修改的代码 / 文档 / 链接）：
- ⚠️ 历史会话、Session ID、外部链接或可读取的其他项目，**不等于**获得该项目的工作授权

## 协作偏好【可选】

> 通用默认见 `CLAUDE.md`；本节覆写项目特有偏好，**覆写优先**。

- 回复语言（通用默认「跟随需求方语言」；固定语言如「始终中文」在此声明）：
- 其他协作偏好（语气 / 详略 / 汇报形态等）：

## 技术栈【必填】

- 语言 / 框架 / 最低版本：
- 状态管理：
- 路由：
- 网络：
- 序列化：
- 本地存储：
- 国际化（如需）：

## 目录与模块职责【可选】

- 主要目录划分与职责：
- 分层约定（如：视图 → 状态 → 数据 → 网络）：

## 构建、分析、测试命令【必填】

> 按改动层选用：跨层改动须覆盖所有触及的层（前端 / 业务 / 数据 / 原生的命令分开列）。

- 拉取依赖：
- 静态分析 / lint（零 warning 才算过）：
- 格式化：
- 跑全部测试：
- 跑单个测试文件 / 单个用例：
- 构建 / 运行：
- 代码生成（如有，如 `.g.dart` / freezed）：

## 模拟器与真机【可选，有 UI 时填】

- 指定模拟器（型号 / 系统 version）：
- 指定真机（验收用）：
- 硬件依赖功能（蓝牙 / 音频 / 传感器 / 后台恢复等，**必须真机验收**）：

## UI 主画布与验收标准【可选，有 UI 时填】

- 设计稿主画布：
- 运行视口：
- 验收字号 / 语言 / 外观模式（常规验收的标准配置）：
- 无障碍 / 多设备 / 多语言 / 横竖屏矩阵（只在明确要求时执行，与常规验收分开报告）：

## 设计规范【可选】

- Design Token / 主题来源：
- 颜色 / 字号 / 间距归口（常量类 / 主题扩展）：

## 验收资料归档【可选】

- 截图 / 录屏 / 日志 / 对比图存放目录（**不混入源码 git**）：

## 提交与发布特殊规则【可选】

- 提交触发词覆写（通用默认：`commit` = 本地提交、`push` = 推送远端、「提交并推送」= 完整流程；如需「单发 `commit` 即完整流程」在此声明）：
- 分支策略：
- commit message 特殊约定（默认见 `CLAUDE.md` §2）：
- 发布 / 打包特殊流程：

## 数据、权限、隐私、硬件红线【可选】

> 通用兜底见 `CLAUDE.md` §一「敏感数据安全兜底」；本节填项目特有的具体红线。

- 敏感数据（密钥 / Token / 凭据 / 私有地址 / 生产数据）处理红线：
- 权限要求：
- 隐私合规：
- 硬件 / 外设约束：

---

## 待确认问题

> 项目启动时还拿不准、需要随进展澄清的问题记在这里：

- 
